# Swarm Atlas

> A top-down, evidence-indexed map of the current Swarm repository for agents and maintainers.

## 1. Purpose and use

Use this document to answer four questions in order:

1. **What product is this?** Read §§3–5.
2. **Where is the authority for a behavior?** Use §§2, 6, and 8.
3. **What crosses a security or durability boundary?** Use §§7 and 10.
4. **Which tests are relevant?** Use the evidence column in §8 and the method in §11. A listed test is evidence that a behavior was inspected or encoded; it is **not** proof of complete, correct, or currently passing coverage.

This atlas is an index, not a replacement for code. Resolve a question by following the cited path and symbol at the atlas revision. Do not infer implementation from a route name, test name, UI label, or this summary alone.

### Status labels

| Label | Meaning |
|---|---|
| **Verified implementation** | Inspected registration or implementation exists at the cited revision. |
| **Policy / contract** | Checked-in text or executable contract declares an invariant; implementation still needs inspection when changing it. |
| **Compatibility** | Registered or retained for migration/backward compatibility; do not add new consumers without an explicit decision. |
| **Experimental / dormant** | Present but not a dependable launch surface. |
| **Retired** | Explicitly outside current product scope. Do not restore incidentally. |
| **Unresolved** | Evidence was insufficient or internally inconsistent; verify before relying on it. |

## 2. Evidence, authority, and revision policy

### Authority hierarchy

For current behavior, prefer:

1. route and service wiring (`swarmd/internal/runtime/daemon.go`, `swarmd/internal/api/server.go`, `server_routes.go`);
2. implementation and storage code;
3. executable contract constants and focused tests;
4. `AGENTS.md` and operator/release scripts;
5. `README.md` and historical checkpoint documents;
6. this atlas.

Tests show intended or guarded behavior, but do not prove production behavior or that the suite passes. Finder reports, transcripts, generated inventories, and issue prose are discovery aids only.

### Atlas revision

- **Implementation as-of commit:** merged working tree based on Artifact V2 candidate `3154606c` and current `dev` `c1462bcc`; Artifact V2 is the current managed creative-write pipeline, while the concurrent fresh-host installer correction and distro installation gates remain part of the integrated source. Managed regular and swarm Designers receive only `artifact_v2_author`, create durable Working Artifacts with immutable Parts/Revisions before validation, repair exact revisions through safe diagnostics, and submit exact Published Heads; their compiled profile does not enable V1 `manage_artifact`. Focused `artifact_v2_source` Designer swarms authenticate one published base, open one durable Iteration Round, atomically import only target revisions, retry bounded optimistic-concurrency conflicts while sibling candidates attach, preserve non-target/locked bytes, and keep candidates out of the accepted head until selection. Desktop reconstructs working state and real part/iteration history from `/v3/sessions/{session_id}/artifact-v2` and uses V2 CAS commands for exact selection and locks. The V2 motion compiler/template owns runtime bytes and rendered evidence; typed storyboard parts and one server-owned `convert_artifact_v2` adapter create pending Video Studio proposals. V1 `create`/`create_package`, managed placeholders/refinement choreography, model-authored runtime/storyboard handoffs, and manual candidate/fallback assembly are retired; historical ready V1 artifacts remain accessible only through a structurally read-only bounded adapter. The concurrent durable Video Studio render audit, startup-recovery declaration correction, testbench model posture, dependency-scan concurrency, and Desktop delegated-task sidebar grouping corrections are recorded in §14.
- **Latest atlas audit input commit:** source `b1d22ef8aee716e4d5c279ba240ade6b8b768b32`, integrated as `8c5ea4232c28a9d365a4b211d0748067dc392d44`.
- **Inspection method:** the earlier independent security/governance and test-promotion audits, followed by current implementation reconciliation of mint/activation/feedback, artifact/video/composition/animation, workspace scope, session worktrees, workspace-aware `/task`, saved-workspace path authority, captured-checkout promotion, the concurrent durable Video Studio renderer, testbench model posture, dependency-scan concurrency, delegated task-call sidebar lineage, fresh-host installer path ordering and temporary-file creation, release archive assembly, distro CI, and testbench Omarchy VM coverage. The Artifact V2 contract freeze additionally inspected the current V1 tool actions, write authority, mutation records, placeholder/refinement orchestration, Desktop projections, trusted renderers, storyboard import, and Video Studio proposal boundary solely to define anti-derivation, quarantine, and retained-primitive gates. Earlier audit history remains in §14.
- **Artifact V3 integration under review:** daemon construction now resolves private `artifacts-v3/git`, `artifacts-v3/worktrees`, `artifacts-v3/evidence`, and `artifacts-v3/video-derivatives` roots, constructs the native Git/Pebble service, injects the context-bound `artifact_v3_author`, registers the authenticated `/v3/sessions/{session}/artifacts-v3` API service, and reconciles persisted repositories at startup (`runtime/artifact_v3_runtime.go`; `runtime/daemon.go`). Browser readiness uses the sandboxed `htmlcapture.Renderer`, requires every server-authored entrypoint selector Part to be visible and fully contained at the exact review viewport, and persists exact PNG digests; non-empty source bytes, bounded document scroll size, or overflow-hidden CSS alone never make a candidate ready. Initial and candidate revisions now retain exact build/render evidence in their durable revision projection, candidate submission moves the turn to `awaiting_selection` without advancing head, and the preview route serves contained revision-bound project assets so conventional HTML/CSS/module trees remain runnable. Primary-Swarm direct HTML creation keeps one Artifact per provider run: exact-byte repeats replay explicitly, while changed visual corrections must preserve stable Part IDs and become a native exact-base child revision selected only after the new build/preview succeeds. For later user turns, `manage_artifact read_v3` authenticates and returns one bounded exact native HTML revision, and `revise_v3` accepts its complete corrected HTML plus exact target Part IDs to publish one build/preview-valid direct-child candidate while leaving the selected head unchanged. One bounded `revise_v3.alternatives` array can carry 2–16 complete exact-base HTML candidates with distinct contiguous indexes; one server-owned call validates and publishes every sibling in the shared turn before reporting success, closing the provider postcondition gap where separate calls could stop below requested cardinality. V3 code has no V1/V2 translation dependency.
- **Artifact V3 runtime audit (2026-09-03):** re-traced daemon construction, managed Designer allocation/run binding, whole-tree authoring, Git/Pebble candidate lifecycle, durable projection publication, native HTTP, and Desktop normalization. Corrected exact revision evidence persistence, multi-candidate `awaiting_selection` transitions, revision-bound project asset serving, and managed handoff projection sequence propagation; focused tests cover the corrected service/runtime/API/client boundaries.
- **Revision rule:** update this hash and the revision ledger whenever a material route, trust boundary, storage authority, client owner, agent/tool contract, critical test manifest, or release gate changes. Re-check every affected citation against code; do not merely edit prose. `AGENTS.md`, “Swarm Atlas Maintenance,” defines mandatory same-change triggers; `scripts/check-atlas-sync.sh` is the conservative static backstop.
- **Line anchors are navigational:** symbols and paths are primary because line numbers drift.

## 3. Mission, goals, boundaries

### Current mission and goals

**Policy / contract.** Swarm is a local-first AI coding workspace composed of an installed launcher, a daemon, a tcell TUI, and a React Desktop UI. Launch priorities are reliable local operation, durable V3 sessions, explicit permissions, authenticated local access, workspace/worktree isolation, provider-neutral orchestration, and portable system installation (`README.md:5-22`; `AGENTS.md`, “Launch Product Scope”).

Current architectural goals visible in code are:

- one durable V3 session authority and atomic mutation path (`session.Service.ApplySessionMutation` in `swarmd/internal/session/session_events.go`; `SessionStore.ApplyV3SessionMutation` in `swarmd/internal/store/pebble/session_event_store.go`);
- sync-first cross-client correctness, with realtime as an accelerator (`V3SyncContractText` in `swarmd/internal/api/sessions_v3_sync_contract.go`);
- code-owned system agents and account-scoped model selection (`BuiltinSystemAgentRegistry` in `swarmd/internal/agent/system_agent_registry.go`; `agentmodelsettings.Service` in `swarmd/internal/agentmodelsettings/service.go`);
- loopback defaults and authenticated non-health access (`startupconfig.Default`; `Server.withAuth`);
- bounded, reviewable workspace execution and isolated delegated Git work (`worktree.Service.AllocateTaskWorkspace`, `PrepareTaskIntegration`, `ApplyTaskIntegration` in `swarmd/internal/worktree/service.go`).

### Non-goals and retired boundaries

**Policy / contract.** `AGENTS.md` is explicit:

- dedicated local-container execution is **retired**;
- a general-purpose Flow product/runtime is **retired**; Workspace Actions are the supported reusable execution surface;
- user-authored custom agents are not a launch product authority; remaining mutable v2 agent/profile APIs are compatibility debt;
- hosted control planes, managed synchronization, remote deployment, and retired runner route-mirroring are not current contracts.

**Experimental / dormant.** Copilot implementation remains in source but is deliberately not registered as a provider or runner (`swarmd/internal/runtime/daemon.go:450-471`). Voice is described as experimental in `README.md:125-137`.

## 4. System context and primary flows

```text
swarm launcher / swarmtui                 React Desktop
 cmd/swarm, internal/app                  web/src/features/desktop
           | authenticated HTTP/WS / trusted owner IPC |
           +-------------------+------------------------+
                               v
                     swarmd API and boundaries
       api.Server: desktop origin -> auth -> vault -> JSON -> routes
                               |
             +-----------------+------------------+
             |                                    |
       V3 session authority                 runtime/tool layer
 session.Service -> Pebble mutation     run.Service -> provider registry
 events/projections/run intents/outbox   permissions -> tools/worktrees
             |                                    |
             +---------- durable state -----------+
                         Pebble + artifact Git
```

### Request-to-session flow

1. The launcher/TUI or Desktop reaches a daemon listener. Defaults are API `127.0.0.1:7781`, Desktop `127.0.0.1:5555`, and peer transport `127.0.0.1:7791` (`pkg/startupconfig/config.go:18-25,135-156`).
2. Desktop requests additionally cross `Server.withDesktopBoundary`; all API surfaces are assembled by `Server.apiMux`, then wrapped by `withAuth`, `withVaultGate`, and `withJSON` (`swarmd/internal/api/server.go:697-735`).
3. A V3 mutation enters `session.Service.ApplySessionMutation`, which delegates to `SessionStore.ApplyV3SessionMutation` (`swarmd/internal/session/session_events.go:84-88`; `swarmd/internal/store/pebble/session_event_store.go:609`).
4. The atomic Pebble mutation updates event/projection state and emits a durable realtime outbox record. The contract requires replay—not the in-memory hub—to repair missed delivery (`V3SyncContractText`, lines 33-60).
5. Desktop or TUI receives a snapshot through bootstrap/hydrate, replay through sync stream, and live frames through `/v3/realtime/stream` (`sessions_v3_sync_contract.go`; `sessions_v3_realtime_contract.go`).

### AI turn and delegation flow

1. `run.Service` resolves the compiled agent, account-scoped model settings, workspace, prompt, tool contract, and permissions (`swarmd/internal/run/service*.go`; daemon wiring at `runtime/daemon.go:474-480`).
2. The provider registry selects an adapter/runner. Registered adapters are Anthropic, Codex, Fireworks, Google, OpenAI, OpenRouter, and Exa; runners exclude Exa and dormant Copilot (`runtime/daemon.go:455-471`).
3. Tool calls pass through runtime policy and workspace/path constraints (`swarmd/internal/tool/`, `swarmd/internal/permission/`, `swarmd/internal/run/service_tools.go`).
4. Regular delegation launches bounded Finder/Coder/Designer waves. Coder work allocates a child branch/worktree from an immutable base and returns a commit for explicit integration (`worktree.Service.ResolveTaskBase`, `AllocateTaskWorkspace`, `PrepareTaskIntegration`, `ApplyTaskIntegration`).
5. Dependent work uses the Task Program stage/job DAG (`run/service_task_program.go`, `service_task_program_scheduler.go`); rapid independent alternatives use Iteration Swarms (`run/service_task_swarm.go`). Durable child lineage/generation and worktree-owner leases are recorded by `store/pebble/delegated_child_rotation_store.go`, rather than existing only in process memory.

## 5. Repository and component map

| Area | Canonical paths | Responsibility |
|---|---|---|
| Launcher/install | `cmd/swarm/`, `cmd/swarmsetup/`, `internal/launcher/` | service lifecycle, installed paths, release apply/update, controller launch |
| TUI | `cmd/swarmtui/`, `internal/app/`, `internal/ui/`, `internal/client/` | tcell application, V3 client/realtime controller, settings and commands |
| Daemon entry/runtime | `swarmd/cmd/swarmd/`, `swarmd/internal/runtime/daemon.go` | dependency wiring, listeners, background services, provider registration |
| HTTP/WS API | `swarmd/internal/api/` | route registration, auth/desktop boundaries, handlers, V3 sync/realtime |
| Durable state | `swarmd/internal/store/pebble/`, `swarmd/internal/session/` | events, projections, run intents, plans, identity, settings, indexes |
| AI runtime | `swarmd/internal/run/`, `agent/`, `agentmodelsettings/`, `provider/` | prompt/tool policy, system agents, model authority, provider adapters |
| Workspace execution | `workspace/`, `worktree/`, `action/`, `discovery/`, `gitstatus/` | bindings, containment, isolated Git work, actions, skills/rules, status |
| Artifacts/media | `artifact/`, `artifactgit/`, `mediastaging/`, `imagegen/`, `htmlcapture/`, `videosource/`, `videoproject/`, `videorender/`, `videotranscription/`, `voice/` | immutable outputs, ingress, generation, deterministic rendering and media workflows |
| Desktop | `web/src/features/desktop/` | browser UI, V3 cache/runtime/realtime ownership, workspace and media UI |
| Operational evidence | `scripts/`, `tests/`, `swarmd/tests/`, `.github/workflows/` | static gates, opt-in tests, E2E/testbench and release workflows |

## 6. Canonical data, storage, networking, and trust model

### Storage

**Verified implementation.** `storagecontract.defaultRoot` sets Linux roots to `/var/lib/swarmd`, `/var/cache/swarmd`, `/run/swarmd`, `/etc/swarmd`, and `/var/log/swarmd`; Darwin roots are under system `/Library` plus `/var/run/swarmd` (`pkg/storagecontract/storagecontract.go:197-228`). `startupconfig.ResolvePath` joins `swarm.conf` under the config root and uses mode `0600` (`pkg/startupconfig/config.go:27-28,127-133`). `storagecontract.Join` rejects root escape (`storagecontract.go:180-185`).

**Verified implementation.** Session correctness state is Pebble-backed. Material records include session events, projections, run intents, messages, media references, and realtime outbox records (`V3SessionRunIntent`, `V3RealtimeOutboxRecord`, and `ApplyV3SessionMutation` in `session_event_store.go`). Delegation also has durable lineage/generation records and worktree-owner leases (`DelegatedChildLineageRecord`, `DelegatedChildGenerationRecord`, and the rotation store in `store/pebble/delegated_child_rotation_store.go`). Managed historical artifacts use private bare Git repositories with isolated Git environment/config; `artifactgit.Repository` disables external config, prompts, hooks, signing, and file protocols (`swarmd/internal/artifactgit/repository.go:82,98`). Native Artifact V3 instead owns one complete-project repository per Artifact through `OpenArtifactV3Repository`, with project-wide file/tree/path/ref quotas and owner binding (`store/pebble/artifact_git_v3_repository.go`). Production roots are daemon-private data/cache children selected by `artifactV3StorageRoots`; host paths never enter API or model arguments.

### Network and authentication

- `Daemon.Run` binds configured API/Desktop/peer TCP listeners and a Unix socket; the owner IPC directory/socket are permission-restricted (`swarmd/internal/runtime/daemon.go:826-917`). Startup validation rejects unsupported non-loopback hosts rather than merely relying on loopback defaults (`startupconfig.validate` in `pkg/startupconfig/config.go`).
- `Server.Handler` applies auth and vault gates; `DesktopHandler` also applies local-session and browser boundary middleware (`api/server.go:710-735`).
- `Server.withAuth` accepts a validated identity session, a narrowly validated artifact-preview request, trusted local transport, or a valid attach token. Otherwise it returns 401 (`api/server.go:3922-3970`). Artifact preview admission is GET-only, AEAD-authenticated, expiration-bounded, and tied to the exact session/artifact content-preview path (`validateSessionV3ArtifactPreviewRequest` in `api/sessions_v3_artifacts.go`).
- Auth exemptions are narrowly enumerated for health/readiness, eligible Desktop bootstrap, initial onboarding, and admitted/pending Tailscale origin approval (`Server.isAuthExemptRequest`, `api/server.go:3984-3998`).
- Browser-origin checks compare admitted/local origin, referer, host/scheme, and `Sec-Fetch-Site` (`isSameOriginBrowserRequest`, `api/server.go:4001-4040`; `api/desktop_boundary.go`).
- A locked vault gates authenticated account routes except the explicit health/readiness, Desktop-session bootstrap, vault-management, and system-shutdown paths enumerated by `isVaultExemptRequest` (`api/vault.go:247-253`). Vault exemption does not itself bypass authentication; middleware boundaries remain separate.

### Principal and scope

The durable scope is account + user/principal + workspace/session. Sync cursors additionally bind surface, stream kind, selector hash, resource set, and scan position (`V3SyncScopeFields`, `V3SyncCursorPayloadFields`). Handlers must derive identity from request context; callers must not supply authority-bearing lineage or filesystem paths unchecked (`swarmd/internal/identity/`; artifact `Principal` in `artifact/authority.go:39`).

## 7. Detailed subsystem maps

### Artifact V3 whole-project authority

- **Git and events:** `pebblestore.ArtifactV3Service` coordinates one complete Git revision with `ArtifactV3*Projection` records committed by `ApplyV3SessionMutation`; candidate and head moves use exact Git refs/CAS and matching build/preview evidence.
- **Managed authoring:** `tool.ArtifactV3AuthorService` exposes only bounded full-tree operations and repeatable `build_preview`/`finish_turn`. `runtime.artifactV3RuntimeAdapter` supplies production repository, builder, browser preview, and turn-coordinator interfaces.
- **API/realtime:** `/v3/sessions/{session}/artifacts-v3` catalog/detail/revision/preview/select handlers use the same runtime adapter. `PublishArtifactV3Projection` writes native `artifact.v3.*` durable events/outbox records without collection/variant or V2 shapes.
- **Recovery/attack points:** startup enumerates only contained `<artifact>.git` children, authenticates their `swarm-owner.json`, runs strict repository recovery, and fails closed on owner, head, transaction, or object disagreement. Preview uses loopback-only sandboxed browser capture, blocks outbound network, and requires immutable PNG evidence.
- **Cutover boundary:** Artifact V3 code does not import or translate `artifactv2`, V1 collection/variant state, `declare_parts`, `write_part`, or per-Part byte authorities. Historical V1/V2 reads remain separate compatibility surfaces.
- **Provider-backed journey gate:** `scripts/runners/artifact-v3-multipart-e2e.mjs` configures canonical models, respects the V3 hydrate cap, resumes exact sessions after harness-only failure, proves static/targeted/continued/alternate/animated no-Designer stages, and now includes a resumable `video-conversion` stage. That stage accepts one exact selected animated head, requires server-owned `create_project` then only `convert_artifact_v3`, inspects the source pixels, verifies a single pending native V3 proposal plus fallback/MP4 identities and bounded digest-verified container/size evidence, requires source-head/replay immutability and zero delegated children or V1/V2 identity, and never accepts or starts final render.

### V3 sessions, plans, sync, and realtime

- **Authority:** `session.Service.ApplySessionMutation` -> `SessionStore.ApplyV3SessionMutation`.
- **Atomic products:** event, projection, run intent/state, message/resources, media/search indexes as applicable, and durable outbox. Commit uses synchronous Pebble batches in `session_event_store.go`.
- **Reads/sync:** `handleSessionsV3SyncBootstrap`, `handleSessionsV3SyncHydrate`, `handleSessionsV3SyncStream`; live WS is `handleV3RealtimeStream`.
- **Protocol:** `v3.realtime`, version 1 (`sessions_v3_realtime_contract.go:13-38`). Cursors are signed/scoped/opaque by contract; clients must not parse or compare cursor numbers (`V3SyncContractText`).
- **Plans:** lifecycle services are in `session/plan_lifecycle_service.go`, `api/sessions_v3_plan_lifecycle.go`, and `run/plan_lifecycle_v3.go`; plan/checkpoint writes must use the session mutation boundary.
- **Recovery:** `api/sessions_v3_executor.go`, `sessions_v3_stale_recovery.go`, and durable run-intent records recover or terminalize interrupted work.
- **Compatibility:** `/v3/sessions:workset` and `/v3/tui/sessions:workset` remain removal-gated by `V3SyncWorksetRemovalGates`. `/v1/sessions*` and `/ws` are legacy surfaces, not the canonical V3 chat path.

### Agents, models, providers, prompts, and tools

- **System agents:** immutable definitions and tool contracts live in `agent/system_agent_registry.go`. `NewSystemAgentRegistry` rejects duplicate/invalid definitions, delegation-enabled subagents, and primary agents without `plan_auto` (`lines 69-120`).
- **Current compiled identities:** 13 definitions—Swarm, Plan and AI sidechats, Compact, Finder, Coder, Designer, Image, Idea, AI Task Preparer, Review Commit, Workspace Definition, and Router (`builtinSystemAgentDefinitions`, `system_agent_registry.go:201-293`; IDs/names at lines 13-39).
- **Model authority:** account-scoped `agentmodelsettings.Service`; canonical route constant `AgentModelSettingsPath = /v1/agent-model-settings` (`api/swarm_mode_settings.go:14-16`).
- **Provider boundary:** generic orchestration stays in `run/`; wire/auth/stream translation belongs in `provider/*`. Registration truth is `runtime/daemon.go:455-471`, not directory presence.
- **Prompt assembly:** `run/service_prompt.go` composes the mandatory harness and agent overlay with workspace scope, then appends backend-authoritative run/checkpoint context from `run_state_prompt.go` and `checkpoint_prompt.go`; inspect those symbols for exact ordering. Runtime and durable state injections override stale transcript claims, but this is a construction/enforcement mechanism—not a claim that arbitrary repository text is intrinsically safe.
- **Tools/permissions:** implementation is under `tool/`, `run/service_tools.go`, `run/service_workspace_scope.go`, and `permission/`. For a principal-backed primary run, `resolveRunWorkspaceScope` re-authenticates every active account-saved workspace and admits its canonical primary path, so `read`, `list`, `search`/`agentic_search`, `find`, `write`, `edit`, `webdownload`, and task workspace selectors need no workspace-scope prompt anywhere in the user's saved workspace pool; unrelated filesystem paths still require session-scoped expansion, and operation policy remains independent. The compiled Coder contract disables bash, delegation, direct user input, session/plan/settings mutations, and enables dedicated Git tools (`CoderAgentToolContract`, `system_agent_registry.go:446-457`). A Coder receives that same authenticated workspace pool only as read-only context, while any mutation outside its owned worktree scope or any unrelated read path is rejected by `ScopeExpansionForCall` before `requestWorkspaceScopePermission` can create a user-facing permission record. Designer has distinct managed-artifact and workspace-edit contracts so the same run cannot silently combine both authorities; Idea is tool-free (`DesignerAgentToolContract`, `DesignerWorkspaceAgentToolContract`, and `IdeaAgentToolContract` in `system_agent_registry.go`). Runtime profiles can narrow capabilities further.
- **Delegation forms:** regular dependency-ready waves are handled by task launch code; Task Programs schedule declared stage/job dependencies and terminalize failures/blockers durably (`service_task_program*.go`); Iteration Swarms fan out Designer/Image/Idea alternatives with mode-specific routing (`service_task_swarm.go`). Child session lineage and generation rotation are persisted in `store/pebble/delegated_child_rotation_store.go`.

### Workspaces, worktrees, actions, discovery, Git

- Workspace records/bindings are Pebble-backed (`store/pebble/workspace_store.go`). The account Workspace Map is a separate lower-authority, revisioned orientation document (`store/pebble/workspace_map_store.go`) injected only into the primary Swarm prompt; `manage_workspace update_map` builds backend-owned canonical approved arguments, requires action-specific `workspace_map_update` authorization, and executes with optimistic revision matching (`run/service_workspace_manage.go`, `run/service_tools.go`). Path containment is a security boundary; inspect `workspace/` and `worktree.pathWithinRoot` before accepting a path.
- `worktree.Service.AllocateTaskWorkspace` owns delegated checkout isolation; integration validates child path, branch, base, and head (`worktree/service.go`).
- Workspace Action definition CRUD (`action.Service.Create/Update/Delete/Reorder`) is separate from execution (`action.Runner`; `api/workspace_actions.go` and `workspace_action_runs.go`). Entrypoints resolve relative to the real workspace and arguments are structured arrays (`action/runner.go:100-104,288-341`).
- Rules/skills are discovered in `discovery.Service`; they guide agents but cannot override runtime policy.
- Git status/watch is in `gitstatus/` and `gitwatch/`; authenticated API mutation surfaces include status, commit, suggestion, realtime, and worktree management.

### Desktop and TUI ownership

- **Desktop:** backend-derived V3 state is owned by `web/src/features/desktop/state/desktop-v3-cache-store.ts` and `desktop-v3-cache-reducer.ts`; realtime orchestration is in `realtime/v3-realtime-controller.ts`; runtime composition is under `runtime/`. Components should consume selectors/actions rather than create another transport cache. Sync shell metadata retains delegated children’s allowlisted `parent_task_call_id`, and the sidebar uses that durable identity to keep siblings from one task call adjacent inside one visual group without consulting transcript content (`sessions_v3_sync_bootstrap.go`, `sidebar-session-lineage.ts`, `desktop-app-page.tsx`).
- **TUI:** `internal/app/app.go` owns app lifecycle. `tuiRealtimeController` in `internal/app/tui_realtime_controller.go` reconciles V3 subscriptions/worksets and resume cursor; `internal/client/` implements daemon calls.
- **Workspace-aware `/task`:** both clients call `/v3/sessions:background-router`. With no selector, TUI resolves the current canonical source workspace and Desktop resolves the active workspace; `--workspace` accepts only an available saved workspace identity. Clients send that workspace's bound self-host authority, while Router receives only task text and returns title/worktree-name seed. The backend replay-checks first, calls Router once, allocates a mandatory new owned worktree in the selected repository, and atomically publishes session/message/run intent. TUI ownership is `internal/app/task_command.go` plus `internal/client/workspace_ai_task.go`; Desktop ownership is `chat/services/task-workspace-selection.ts`, `layout/desktop-app-page.tsx`, and `session-v3/write-api.ts`.
- Both surfaces must repair gaps from durable sync. UI-local state and in-memory hubs are not session authority.

### Artifacts, media, image, HTML, video, voice

- **Verified legacy/read implementation (V1).** Managed artifact authority and lineage are in `artifact/authority*.go`; bytes/manifests are in private `artifactgit` repositories. Workspace materialization must pass destination containment (`artifact/materialize.go`). Legacy Desktop viewing still infers turns, progress, and accepted heads from V1 collection/variant/step/composition records (`artifact-api.ts`, `artifact-studio-model.ts`, `desktop-v3-artifact-sidebar.tsx`, `desktop-v3-artifact-gallery.tsx`). New managed Designer work is separately projected through Artifact V2 in `artifact-v2-api.ts`, `artifact-v2-studio-model.ts`, `desktop-v3-artifact-v2-sidebar.tsx`, and `desktop-v3-artifact-v2-studio.tsx`; V1 remains a deletion/quarantine and bounded legacy-read target, not its implementation scaffold.
- Pre-session media ingress is `mediastaging.Service`: put/read/delete/expire, bounded `CleanupExpired`, account-preflighted `CleanupAbandoned`, and atomic binding (`mediastaging/service.go:50-196`).
- Image generation is `imagegen.Service`. `ManagedImageCapabilities` issues a capability snapshot/token and generation validates it; managed parallelism is bounded to 4, Gemini slots to 10 (`imagegen/service.go:35-36,197-252,568`; `gemini.go:322`).
- **Verified current implementation (retained primitive candidates plus V1 callers).** HTML still capture enforces `swarm.capture/v1` and 1920x1080 (`htmlcapture/renderer.go:36,268-285`). Animation HTML declaring `swarm.animation/v1` cannot publish ready without a reviewed profile and trusted preflight; `motion_ui` additionally proves artifact-owned requestAnimationFrame playback before deterministic ready/seek/stability/viewport checks (`tool/runtime_manage_artifact_animation.go`, `htmlcapture/animation.go`). Production animation rendering uses two to four isolated browser pages according to the server-owned quality preset, a bounded ordered in-memory PNG window, and one persistent backpressured FFmpeg `image2pipe`. Artifact V2 may retain audited Chrome/encoding primitives only through V2-owned compiler/renderer interfaces; V1 manifests, model-authored bootstrap, tool runtime, publication flow, and prompts remain forbidden dependencies.
- Video sources use opaque references (`videosource/`); projects/proposals/revisions and render-job creation/listing use `videoproject.Service`; background composition, cancellation, and restart recovery use `videorender.Service`; transcription/analysis is `videotranscription/`. Render jobs are V3 mutations pinned to account/user/session/project/revision plus allowlisted quality and 30/60 FPS, are admitted through a bounded concurrent service semaphore, retain durable progress/ETA/output lineage, and reuse an exact ready MP4 for the same revision/settings. `AcceptEditProposal` is a distinct operation from proposal creation. Native Artifact V3 conversion is owned by `artifactv3video.Service` plus `runtime.artifactV3VideoBridge`: the model supplies only exact V3 session/artifact/revision and project-base identity; the server authenticates the selected Git head/build/validation, injects deterministic render timing into cloned bytes, distinguishes Git SHA-1 OIDs from SHA-256 content digests, validates renderer-observed duration/FPS and PNG/MP4 container signatures, atomically stores digest-bound derivatives, and creates one pending proposal. `videorender.Service` materializes only the exact authenticated V3 MP4 derivative. Artifact V2 remains a separate restricted pending-proposal sink.
- Voice/STT is in `voice/` and provider adapters. Treat local model path resolution and uploaded audio as untrusted input.

## 8. API catalog

### How to read this catalog

All listed routes are registered by the eight `register*Routes` methods called by `Server.apiMux` (`api/server_routes.go`; `api/server.go:697-707`). Unless an exemption below applies, they inherit auth + vault + JSON middleware; Desktop access additionally inherits local-session and browser-origin boundaries. Methods are enforced inside handlers, so inspect the handler before changing or calling a route. “Tests” means related inspected files, not a coverage assertion.

| Route family / registrations | Handler/service and primary consumers | Scope / lifecycle | Related inspected tests |
|---|---|---|---|
| `/healthz`, `/readyz` | `handleHealth`, `handleReady`; launcher/health supervision | auth-exempt; **current** | `api/server_json_test.go`, `api/desktop_unbootstrapped_test.go` |
| `/ws` | `handleDesktopStream`; legacy stream clients | authenticated; **compatibility** | `sessions_v3_realtime_strict_test.go`, `sessions_v3_realtime_contract_test.go` |
| `/v1/auth/codex`, `/v1/auth/codex/oauth/{start,status,complete}` | `handleCodexAuth`, OAuth handlers; onboarding/settings | account secret; **current** | `provider/codex/oauth_device_test.go` (handler implementation: `api/codex_oauth.go`) |
| `/v1/auth/credentials{,/verify,/active,/delete}` | credential handlers and auth store; onboarding/settings/provider picker | account secret; **current, critical** | `api/auth_cleanup_test.go`, `auth_v3_realtime_test.go`, `onboarding_provider_test.go` |
| `/v1/auth/desktop/session`, `/v1/auth/attach/rotate` | local browser bootstrap / daemon attachment | first has narrow GET exemption; **current, critical** | `desktop_local_auth_jwt_test.go`, `protected_identity_guard_test.go` |
| `/v1/vault{,/enable,/unlock,/lock,/disable,/export,/import}` | `handleVault*`; Desktop vault settings | account secret/crypto; **current, critical** | `local_auth_substrate_test.go` (gate implementation: `api/vault.go`) |
| `/me`, `/v1/me`; `/v1/onboarding`; Tailscale approval; `/v1/onboarding/provider/credential`; `/v1/account/*` | identity/onboarding/account handlers; setup and settings | narrow onboarding exemptions; **current** | `onboarding_security_test.go`, `desktop_boundary_test.go`, `account_username_test.go` |
| `/v1/agent-model-settings` | `handleAgentModelSettings` -> account model settings; Desktop/TUI model controls | account-scoped canonical authority; **current** | `swarm_mode_settings_test.go`, `agentmodelsettings/service_test.go` |
| `/v1/swarm/targets`, `/v1/swarm/target/{current,select}`, `/v1/swarm/groups*`, `/v1/swarm/topology*` | target/group/topology handlers; routing and topology UI | account/workspace; **current generic target contract** | `swarm_targets_test.go`, `topology_account_route_test.go`, `topology_workspace_bindings_test.go` |
| `/v2/custom-tools*`, `/v2/agents*` | `handleCustomToolsV2`, `handleAgentsV2`; legacy profile consumers | account; **compatibility/deprecated authority** | `agent_v2_contract_test.go`, `agent_v2_summary_test.go`, `agent_v2_visibility_test.go` |
| `/v1/codex/account/{usage,reset-credits,reset-credits/consume}` | Codex account handlers; usage/settings | account/provider; **current** | `codex_usage_test.go`, provider Codex tests |
| `/v1/image/{providers,generations,assets,storage/reveal}`, `/v1/media/settings/catalog` | image/media handlers -> `imagegen.Service`; Desktop media tools | account/workspace; external generation is permission-sensitive; **current** | `image_generation_test.go`, `image_threads_test.go`, `media_settings_test.go`, `media_reveal_test.go` |
| `/v1/model`, `/v1/model/catalog{,/check}`, `/v1/model-profiles*`, `/v1/models/favorites*`, `/v1/providers` | model preference/catalog/profile handlers; model controls | account; **current** | `model_profiles_test.go`, `agentmodel/resolver_test.go` |
| `/v1/stt/transcribe`, `/v1/voice/*` | STT/voice handlers -> `voice.Service`; TUI/Desktop voice | account/device/media; **experimental product surface** | `voice/*_test.go`, provider transcription security tests |
| `/v1/ui/settings`, `/v1/settings/tailscale{,/approve,/revoke}`, `/v1/onboarding/tailscale-origin` | UI/Tailscale handlers; Desktop/TUI settings and origin approval | account/network; onboarding approval has a narrow exemption; **current** | `ui_settings_test.go`, `tailscale_settings_test.go`, `desktop_boundary_test.go` |
| `/v1/workspace/{resolve,select,current,list,overview,cwd/resolve,discover,browse}` | workspace handlers -> workspace store/service; launcher/Desktop/TUI | account + contained path; **current, critical** | `workspace_cwd_resolve_test.go`, `workspace_add_self_binding_test.go`, `workspace/path_containment_security_test.go` |
| `/v1/workspace/{folders/create,add,directories/*,source-media/*,theme,icon,rename,move,delete}` | workspace mutation handlers; workspace settings/sidebar | account + workspace generation/path; **current** | workspace/api focused tests; `workspace/source_media_directories_test.go` |
| `/v1/workspace/todos`, `/v1/workspace/skills/delete`, `/v1/workspace/definitions/refresh` | todo/discovery handlers; workspace panels/runtime prompt preparation | account + workspace; **current** | `workspace_todos_test.go`, `workspace_definition_refresh_test.go`, `discovery/*_test.go` |
| `/v1/workspace/actions`, `/v1/workspace/actions/run`, `/v1/workspace/actions/runs{,/cancel}` | definition service vs action runner; Actions UI | account/workspace; execution is a separate user gesture; **current, execution-critical** | `api/workspace_actions_test.go`, `store/pebble/action_store_test.go` |
| `/v1/workspace/git/{status,commit,commit/suggestion,realtime}`, `/v1/worktrees`, `/v1/manage-worktree` | Git/worktree handlers; Desktop Git and delegation integration | contained repository + principal; **current, VCS-critical** | `git_commit_test.go`, `git_realtime_test.go`, `worktrees_test.go`, `worktree/*_test.go` |
| `/v1/workspace/video/{scan,transcribe*,storage/reveal,projects*,threads*}`, `/v1/workspace/audio/{transcribe*,analysis/read}`, `/v1/workspace/image/threads*` | media/video/image thread handlers; Desktop media studios | account + workspace + opaque source refs; **current** | `audio_transcription_test.go`, `video_security_test.go`, `video_studio_integration_test.go`, `image_threads_test.go` |
| `/v1/context/sources`, `/v1/permissions*` | context and permission handlers; runtime controls | account/session; **current, critical** | `permissions_*_test.go`, tool policy tests |
| `/v1/feedback` | `handleDesktopFeedback` -> authenticated `swarm.PublicAPIClient`; Desktop feedback form | authenticated local account; forwards bounded category/message/form-time with the mint-issued activation credential; **current** | `swarm/public_api_client_test.go` (transport/auth contract); handler method/error logic inspected in `api/feedback.go` |
| `/v1/system/shutdown`, `/v1/update/{status,apply,run}` | daemon lifecycle/update handlers; launcher/Desktop | authenticated local admin; update apply/run require owner IPC or admitted same-origin Desktop—not an ordinary remote attach token; **current, operationally critical** | `update_test.go`, `update_helper_launch_test.go` |
| `/v1/alerts*`, `/v1/notifications*`, web-push prefix | notification handlers/realtime resources; Desktop | account/user; **current** (`alerts` appears alias-like; inspect before removal) | `webpush_test.go`, notifications store/API tests |
| session maintenance, `/v3/developer/session-dump`, `/v3/developer/session-dump/file/{generated-name}`, long-diagnostics constants | maintenance/diagnostic handlers; developer/operator workflows | authenticated and config-gated; **current bounded operations**. Session dump and its exact generated-file download are `dev_mode`-only. Download accepts only a server-issued basename rooted in the private dump directory and rejects traversal/symlinks. Session lookup still resolves a supplied session ID without an account-scope equality check, so development mode intentionally/operationally weakens tenant isolation | `session_storage_maintenance_test.go`, `session_dump_test.go`, `long_session_diagnostics_test.go` |
| `/v1/integrations*` | integration handlers; integration workspace UI | account/workspace; **current** | `integrations_test.go`, `integration_sessions_test.go` |
| media staging constant and item suffix | `handleMediaStagingCollection/Item` -> `mediastaging.Service`; composer uploads | account + TTL; **current** | `media_staging_test.go`, `mediastaging/service_test.go` |
| `/v3/sessions:routed`, `/v3/sessions:background-router` | `handleRoutedSessionStart`, `handleBackgroundRouterSessionStart`; `/task` consumers are TUI `CreateRoutedTaskSession`/`handleTaskCommand` and Desktop `postDesktopV3BackgroundRouterSessionStart`/`resolveDesktopTaskWorkspace` | account + selected saved/current source workspace + V3 atomic mutation; background Router replay-checks before one tool-free Router call, uses Router title/worktree seed, and always allocates a new session-owned worktree; Router never selects workspace; **current, execution-critical** | `background_router_sessions_test.go`, `session_router_test.go`, `internal/app/task_command_test.go`, Desktop task/workspace/write API specs |
| `/v3/sync/{bootstrap,hydrate,stream}` | sync handlers; Desktop/TUI recovery and hydration | signed exact scope; **current canonical sync** despite stale “future” comments | `sessions_v3_sync_*_test.go`, contract test |
| `/v3/realtime/stream` | `handleV3RealtimeStream`; Desktop/TUI live/replay client | `v3.realtime` v1, signed scoped cursor; **current canonical live transport** | `sessions_v3_realtime_*_test.go` |
| `/v3/sessions` and `/v3/sessions/` | `handleSessionsV3Primary` owns collection operations; `handleSessionV3PrimaryByID` and `parseSessionsV3PrimaryPath` dispatch nested messages, runs/streams, plans/plan mode, permissions, artifacts, video, media, settings/preferences/model profile, metadata/title/usage, archive and compaction; Desktop/TUI/session API | account/user/workspace + mutation authority; **current canonical session API**; inspect both handlers and parser before adding/changing suffixes | `sessions_v3_primary_test.go`, resources/model/plan/media tests |
| `/v3/sessions/{id}/video/projects/{project}/render`, `/v3/sessions/{id}/video/render-jobs`, `/v3/sessions/{id}/video/render-jobs/{job}{,/cancel}`, project `/render-jobs` | `handleSessionV3VideoSubpath` / `handleSessionV3VideoProjectDetail` → `videoproject.Service` and `videorender.Service`; Video Studio render dialog/center | authenticated account + user + session/project/revision; durable V3 queued/rendering/terminal lifecycle, allowlisted quality/FPS, bounded concurrent background execution, list/status/ETA/cancel/recovery and exact ready derivative reuse; **current** | `sessions_v3_video_projects_test.go`, `videoproject/service_test.go`, `videorender/service_test.go`, `htmlcapture/animation_test.go`, Desktop `video-render-center.spec.ts` and `video-tool-page.static.spec.ts` |
| `/v3/sessions:{reconnect,discover,search,archive,review-worktrees,unarchive,delete}`, `/v3/subagents:stop` | specialized V3 lifecycle handlers; clients and task UI | account/session; **current** | matching `sessions_v3_*_test.go` files |
| `/v3/artifacts` | session artifact handler; Desktop artifact gallery/tools | principal/session lineage; **current** | `sessions_v3_artifacts_test.go`, artifact contract tests |
| `/v3/sessions:workset`, `/v3/tui/sessions:workset`, `/v3/tui/sessions*` | legacy workset/TUI handlers | **compatibility; explicit removal gates** | `sessions_v3_sync_contract_test.go`, `sessions_v3_workset_test.go`, `sessions_v3_tui_test.go` |
| `/v1/sessions`, `/v1/sessions/` | legacy handlers | **compatibility; not V3 rendering authority** | legacy API tests; V3 contract guards |

### Consumer verification rule

Before deleting or changing a route, search exact route constants/strings in `web/src`, `internal/app`, `internal/client`, scripts, and tests. Registration proves exposure; it does not prove a current production consumer. In particular, the checked-in sync contract itself says legacy workset removal requires static guards, runtime logs, cursor/snapshot/outbox tests, and live cross-client E2E (`V3SyncWorksetRemovalGates`).

## 9. Operational and release gates

- `scripts/check-precommit.sh`: formatting/static policy, path/secret/privacy/dependency checks, plus `scripts/check-atlas-sync.sh`; the legacy broad Go test path remains opt-in (`README.md:201-208`; script source).
- `scripts/run-critical-tests.sh {fast,deep,agents,all}`: the explicit atlas-driven executable manifest. `fast` covers deterministic listener, authentication/vault, authority, containment, media/image/video, FFmpeg-input, workspace-aware background `/task` admission, TUI/client routing, and critical Desktop V3 state/realtime/task-routing failures; `deep` covers Pebble mutation/outbox/replay/restart/concurrency, executor idempotency, delegated Git integration, and artifact Git integrity; `agents` covers system-agent identity/tool authority, launch trust rejection, Task Program and Iteration Swarm scheduling, subagent capacity/approval, durable child lineage/recovery, and Desktop child/realtime presentation. Every invocation is package/function-bounded, verifies each declared Go package/pattern resolves at least one test before execution, and uses explicit Go/Node test timeouts.
- `scripts/check-prepush.sh`: pre-push gate for protected branches; runs precommit plus the critical `fast` suite and must not be bypassed.
- `.github/workflows/critical-tests.yml`: checks out the declared PR/push head, installs the required repository-policy search tool, fetches the exact declared comparison commits, then runs repository policy/secret/path/atlas checks plus all three critical tiers on `dev`/`main` pull requests and `dev` pushes. `.github/workflows/install-distro-smoke.yml` builds one exact candidate and verifies clean Ubuntu and Arch systemd installs from curl availability through active service, CLI invocation, unset `TMPDIR`, and canonical runtime ownership. `.github/workflows/dependency-vulnerability-scan.yml` independently runs Go/web dependency scanners on PRs, protected pushes, and a schedule; concurrency is event-scoped as well as exact-SHA-scoped so a pull-request scan cannot cancel the required push scan for the same candidate, while duplicate runs of one event type still supersede older in-progress work. Current dependency evidence requires the `golang.org/x/crypto` fix for CVE-2026-56854. `.github/workflows/build-main.yml` reruns `all`, the archive smoke, and Ubuntu/Arch distro installs before publishing a release candidate.
- `scripts/check-launch-readiness.sh --require-clean`: composes launch defaults with cleanliness/public-repo checks (`scripts/check-launch-readiness.sh`).
- `scripts/testbench-container-deploy.sh`, `scripts/lib-testbench-e2e.sh`, and `docs/testing/testbench-container.md`: the sole live candidate-test authority is the fixed-broker, isolated `systemd-nspawn` machine. Exact clean committed candidates are transported as verified Git bundles, built and installed only under container-local system paths, and exposed through remote loopback Desktop/API ports `5655/7881`; `check` requires the active container's `candidate_head` to equal current `HEAD`, while `run` automatically invokes that same exact-candidate deployment when the container is stopped or stale before opening its temporary forwards. The ignored `.env` declares `SWARM_TESTBENCH_TARGET=container`; wrappers reject host ports `5555/7781`, and `testbench-e2e-tunnel.sh` is only a compatibility delegate with no host-service deployment or restart fallback.
- `scripts/run-testbench-launch-prerun.sh`: canonical bounded launch suite manifest for the local deterministic `critical` gate plus onboarding, Desktop, TUI, plan-auto, task routing, Task Programs, provider sync/realtime, and an explicit opt-in `omarchy-install` exact-archive lane (`--list-suites` is the manifest authority). Omarchy has no official OCI image: its lane targets a clean throwaway VM overlay produced by the official ISO's unattended `cidata` path, while `scripts/test-install-distro.sh` owns automated official-image Ubuntu/Arch coverage. The `task-routing` lane exercises Auto and Plan from the current workspace, a configured second saved workspace, and an existing-session context; each result must retain the selected binding/source, create a distinct session-owned worktree, publish its durable user message/run intent, and then stop any still-active bounded probe. The focused `designer-artifact-flow --stage regular3` runner pins and restores exact Swarm/Designer assignments, asks for one regular three-Designer wave, and requires exactly three distinct ready text/html animations from exactly three child sessions and one task call; every output must retain the same exact three-part temporal manifest with no failed/unavailable candidate or replacement child. Provider usage is recorded when it arrives, while provider-response completion remains covered by the separate provider-sync lane. The critical lane runs from the candidate checkout; the other lanes exercise the configured loopback-only testbench. Non-secret `.env` role assignments are Fireworks-only and pin the bounded live proof posture independently for Auto, Plan, Coder, and Designer so execution never depends on mutable catalog recommendations or a Codex fallback. `scripts/render-testbench-model-config.sh` renders that exact posture for installation as the root-owned testbench release-gate configuration; local wrappers and automatic/focused remote gate cycles therefore use the same validated Action/Plan model IDs and thinking levels. Fireworks assignments omit `service_tier` so the catalog's default standard path applies; runners must not reuse Codex's `fast` tier. Runtime usage assertions normalize only Fireworks's provider response/request path to its final catalog-ID segment while retaining exact provider and run lineage checks.
- `docs/main-deploy-checklist.md` and release workflows are the publication authority when present at the target revision.

These gates are commands, not timeless claims that the repository passes. Revision 4 records the exact focused executions performed when this manifest was introduced; later revisions must record their own evidence.

## 10. Security- and durability-critical matrix

| Critical area | Invariant / likely attack point | Authority | High-value test evidence to inspect |
|---|---|---|---|
| Listener exposure | normal API/Desktop defaults remain loopback; wildcard/LAN listen addresses fail startup; remote origin admission is explicit | `startupconfig.Default`, `config.validateLoopbackListenAddr`, `runtime.Daemon.Run`, `api/desktop_boundary.go` | curated `TestParseRejectsNonLoopbackListen`; `desktop_boundary_test.go` |
| Authentication/bootstrap | exemptions cannot widen; attach/session tokens and preview tickets cannot cross scope | `Server.withAuth`, `isAuthExemptRequest`, `desktop_local_auth.go` | `desktop_local_auth_jwt_test.go`, `protected_identity_guard_test.go`, onboarding security tests |
| Vault/secrets | locked-state and account-secret boundaries fail closed; exemptions hand off to separate auth middleware; logs/errors do not leak credentials | `api/vault.go`, `store/pebble/auth_vault.go`, privacy package | curated `vault_gate_security_test.go`; `local_auth_substrate_test.go`; `panic_privacy_test.go` is a static sink guard, not runtime panic proof; auth cleanup tests |
| Session atomicity | no persistent V3 mutation bypasses the canonical boundary; event/projection/outbox stay atomic/idempotent | `session_events.go`, `session_event_store.go` | `session_events_test.go`, `session_event_store_test.go`, checkpoint/routed atomicity tests |
| Sync/realtime | cursors are signed/scoped/opaque; races cannot fall between snapshot and replay; hub loss repairs durably | `V3SyncContractText`, sync/realtime handlers | sync strict/contract/cursor tests, realtime strict/WS tests |
| Run recovery/plans | durable intents/checkpoint attempts do not duplicate, vanish, or falsely succeed after restart | `sessions_v3_executor.go`, stale recovery, plan lifecycle services | executor idempotency, durable progress, stale recovery, plan invariant tests |
| Account/principal isolation | production session reads/writes filter account and user; supplied IDs cannot widen authority. Explicit exception to track: the `dev_mode` session-dump operator endpoint selects by session ID without account-scope equality enforcement | identity context + store selectors/mutation checks; `api/session_dump.go` | topology account, protected identity, session access/scope tests; `session_dump_test.go` |
| Workspace containment | traversal, symlink, linked-root, and materialization paths cannot escape approved roots; principal-backed scope resolution re-authenticates every active account-saved workspace before admitting its canonical primary path without a workspace prompt; unsaved paths still require session expansion; delegated Coders receive the saved pool read-only and cannot widen mutation or unrelated read scope through user permission, failing before a pending permission is created | `storagecontract.Join`, `workspace.Service.ScopeForPathForPrincipal`, `run.resolveRunWorkspaceScope`, `run.mergeAccountSavedWorkspaceRoots`, `tool.ScopeExpansionForCall`, `run.gateWorkspaceScopeCalls`, workspace/worktree/action/artifact path resolvers | `workspace/path_containment_security_test.go`, `tool/workspace_scope_request_test.go`, `run/service_workspace_scope_test.go`, discovery containment, artifact promotion security, sparse worktree tests |
| Workspace Map authority | the map is account-isolated, lower authority than system/workspace instructions, visible only to primary Swarm, and mutations require exact revision, explicit intent, action-specific authorization, canonical approved arguments, and zero filesystem change; automatic approval must preserve the same canonical payload used by interactive approval | `pebblestore.WorkspaceMapService`, `run.buildManageWorkspacePermissionPayload`, `run.gateToolCalls`, `run.executeControlPlaneToolWithLifecycleRunContext` | `TestManageWorkspaceMapInspectUpdateRefreshAndStaleRejection`, `TestManageWorkspaceAutomaticPolicyApprovalReturnsCanonicalMapArguments` |
| System installation | clean privileged provisioning creates every user-owned parent before its children; caller `TMPDIR` is optional; existing non-writable or unsafe targets still fail closed; exact candidates must install/start/invoke on supported distro boundaries | `install.sh` `provision_system_paths`/`provision_owned_dir`/`provision_tmpfiles_config`; `internal/launcher.systemInstallDirSpecs`/`ensureDirsPrivileged`; distro and Omarchy VM runners | `TestSystemInstallDirSpecsProvisionOwnedParentsFirst`; `.github/workflows/install-distro-smoke.yml`; `scripts/test-install-distro.sh`; `scripts/test-install-omarchy-vm.sh` |
| Command/action execution | definitions do not execute; entrypoints are relative/contained; argv is structured; run snapshots are exact-scope; permission decision is explicit | `action.Service`, `action.Runner`, API run handlers, permission/tool runtime | `action/runner_test.go`, `api/workspace_actions_test.go`, permissions/bash-profile tests, tool runtime tests |
| Session-owned Git isolation | every ordinary Desktop routed, background Router, or deployed Git-backed session allocates an owned managed worktree before durable run authority is published; ordinary Desktop start has no direct `worktree_mode=off` path, invokes the hidden tool-free Router once after replay detection for only title/worktree naming, rejects client worktree naming, preserves source checkout identity as read-only context, and retries one canonicalized name collision before failure; `/task` resolves current/explicit saved workspace before Router and never gives Router workspace choice; later mutation failures roll back allocation; provider start revalidation of exact owner, source/runtime path separation, branch, and effective CWD remains runtime authority | `api.handleBackgroundRouterSessionStart`, `api.handleRoutedSessionStart`, `api.routeSessionOnce`, `web/.../desktop-v3-new-session-pane.tsx`, routed allocation/metadata helpers, and `run.validateMandatorySessionWorktree` | `TestRoutedSessionContractRegression`, routed atomicity/media/replay/collision tests, `desktop-v3-new-session-routed.spec.ts`, `routed-contract-regression.spec.ts`, `desktop-app-page.routed-activation.spec.ts`, plus task-routing/TUI/client live and focused evidence |
| Delegated Git integration | child base/head/branch/path and clean commit lineage are verified before integration; promotion may leave the active owned lane only for the source session's exact captured checkout after principal-backed account ownership and canonical-root equality are revalidated, then cherry-picks the captured-base commit range onto the exact current clean target branch/full HEAD; the historical captured base authenticates the source commit range and is not incorrectly compared to an advanced current target HEAD; stale source/target and dirty-target rejections report the inspected branch/full HEAD needed for a bounded retry without weakening no-apply guards | `api.promoteSessionsV3ReviewWorktrees`, `tool.Runtime.manageWorktreePromote`, `manageWorktreeResolvePromotionTarget`, `workspace.Service.ScopeForPathForPrincipal`, `worktree.Service` integration methods | `sessions_v3_review_worktrees_test.go` advanced-target captured-checkout regression, `runtime_manage_worktree_test.go` exact captured/foreign-target, stale-lineage, and dirty-target no-apply promotion tests, worktree service/sparse tests, routed worktree integration tests |
| Provider boundary | credentials are account-scoped; adapters cannot bypass generic tool/session policy; dormant providers stay unregistered; structured tool results retain their canonical tool name through V3 input normalization so OpenAI-compatible adapters such as Fireworks/Kimi can bind each result to its preceding call without reconstructing private output; Fireworks media continuation binds the session contract to normalized account/provider/type/API-key material rather than a mutable credential-record ID, so equivalent duplicate records remain compatible while changed key material fails closed | `runtime/daemon.go`, `provider/registry`, `api/sessions_v3_executor.go`, `run/service.go`, `provider/fireworks/media.go`, `provider/fireworks/runner.go`, provider adapters | provider runner/auth tests, Fireworks equivalent-record and changed-key media-contract test, named function-output continuation tests, system agent runtime-contract tests |
| System-agent authority | compiled identities/tool contracts cannot be widened by saved profiles, mutable snapshots, or launch metadata; zero-tool and managed/workspace variants stay separated | `agent/system_agent_registry.go`, `run/agent_contract.go`, `run/service_tools.go` | curated registry completeness/rejection/reconciliation, Router/Idea tool-free, subagent task-off, and runtime contract tests |
| Delegation trust and capacity | inherited context is redacted and framed untrusted; callers cannot inject trust fields; only parents may delegate; wave budgets and regular/swarm concurrency remain distinct | `run/service_tools_privacy.go`, `run/service_task_launch.go`, `permission/subagent_reservation.go` | curated privacy/trust-field rejection and reservation denial/budget/limit/failure-release tests |
| Task Programs, swarms, and child lineage | dependencies unlock only after integrated prerequisites; blockers terminalize; swarm modes cannot cross authority; child handoffs/leases survive restart with single-winner rotation; managed outputs must match the exact durable iteration group, candidate index, revision round, source, and ordered selected-target identity allocated before child execution. A regular managed Designer partial wave retains only successful ready references and advertises one bounded replacement-wave action solely when every failed slot is an `animation_inspection_failed` visual rejection with a retained report; trust, publication, infrastructure, mixed, and Task Program failures stay fail-closed | `run/service_task_program*.go`, `run/service_task_swarm*.go`, `run/service_tools.go`, `store/pebble/task_program_store.go`, `delegated_child_rotation_store.go` | curated scheduler/blocker/handoff/swarm-mode tests plus managed artifact placeholder/lineage and fail-closed visual replacement-classification tests, Pebble reopen/concurrency lineage tests, and Desktop bounded child-state tests |
| Artifact V2 canonical domain, Designer authoring, and Desktop projection | New managed writes use a V2-owned durable domain with Working Artifacts, real immutable Part Revisions, CAS Composition Heads, exact Build/Validation Results, Iteration Rounds, Published Heads, and a structurally distinct exact ready read/render boundary. `submit_candidate` idempotently publishes the exact validated head. Focused `artifact_v2_source` swarms authenticate the published base and atomically attach only target revisions to one parent Iteration Round; candidate artifacts remain separate published evidence until explicit selection. Context-bound Designers cannot express destination/policy/profile/renderer/legacy identity overrides; bytes persist before validation, invalid candidates remain visible and repairable, and regular/swarm allocation creates real Working Artifacts rather than V1 placeholders. The authenticated V2 session API directly reconstructs catalog/Studio state; Desktop groups lifecycle states, parts, diagnostics, candidates, exact selection, and locks without V1 collection/variant inference. Likely attacks are V1 import/alias/wrapper/fallback, forged producer/account scope, stale base/head/click, partial wave loss, client-derived accepted state, and mismatched validation evidence | `artifactv2.Service`, `artifactv2.AuthorService`, `artifactv2.GitBlobStore`, `api/sessions_v3_artifact_v2.go`, `tool/runtime_artifact_v2_author.go`, `run/artifact_v2_designer.go`, `agent/system_agent_registry.go`, `store/pebble/session_artifact_v2_{store,mutation}.go`, `session/artifact_v2.go`, `web/.../artifact-v2-{api,studio-model}.ts`, `desktop-v3-artifact-v2-{sidebar,studio}.tsx`; `docs/checkpoints/artifact-v2-constitution.md` remains the contract. Storyboard specialization and the typed pending Video Studio conversion adapter are implemented; final parent/V1 cutover remains checkpoint 7 | `TestArtifactV2SessionCatalogShowsAllocatedWorkImmediately`, `TestArtifactV2StaleCandidateSelectionChangesNoHead`, Desktop `artifact-v2-*.spec.ts`, plus author/durability/CAS/anti-dependency tests; focused tests pass, independent ledger review remains pending |
| Legacy managed artifacts (V1 read compatibility only) | Historical exact ready collection/variant references remain viewable and may be boundedly read or materialized without overwrite. New V1 create/create_package registration is absent and direct calls fail closed; create/update/finalize/select/accepted-head/placeholder/correction/model-publication behavior cannot participate in managed Designer writes. The compatibility adapter's method set has no mutation, selection, validation, publication, conversion, allocation, V2, Video Studio, or generic session-mutation authority | `artifactlegacy.Reader`, retained V1 metadata/byte readers in `artifact/authority.go`, `artifactgit/repository.go`; provider `manage_artifact` exposes read/search/materialization and image-specific operations but not general creative create/create_package | `TestReaderMethodSetIsStructurallyReadOnly`, `TestReaderRejectsNonReadyAndInvalidBoundsBeforeSourceMutation`, `TestArtifactV2CutoverHasNoLegacyDesignerWriteRegistration` |
| Media/image ingress | staging is account-scoped and cleanup cannot delete foreign/bound records; MIME/digest/size/capability/TTL and managed-path checks precede durable binding or storage | `mediastaging`, `imagegen`, session media store | curated account-preflight/bound-record, non-PNG no-write, and managed-path rejection tests; session media tests |
| HTML execution/render | Artifact V2 Designers author strict `artifact.motion.scene/v1` and bounded `artifact.motion.behavior/v1` parts only. `MotionCompiler` deterministically supplies the animation/iteration manifests, parser-time binder, ready/seek/stop runtime, shared render timeline, live scheduler, player bridge, reduced-motion handling, and fixed 1920×1080 stage. Build/validation evidence binds compiler/template/policy/source digests/output digest and representative timestamps. `MotionValidator` and exact-head derivative creation access trusted Chrome only through `MotionRenderer`; safe diagnostics target phases, parts, locators, timestamps, bounds, and lifecycle proofs. Failed fallback/MP4 work is durable and cannot mutate the source composition/published head. Likely attacks are model-owned runtime ABI, V1 tool/publication fallback, caller settings, network/file escape, stale build/policy, unstable/overflow output, and derivative demotion | `artifactv2/motion.go`, `artifactv2/author.go`, `artifactv2/service.go`, `store/pebble/session_artifact_v2_{store,mutation}.go`, `api/sessions_v3_artifact_v2.go`, Desktop `artifact-v2-api.ts` and `desktop-v3-artifact-v2-studio.tsx`; audited primitive `htmlcapture/animation.go`; V1 caller `tool/runtime_manage_artifact_animation.go` remains forbidden | `TestMotionCompilerOwnsDeterministicRuntime`, `TestMotionCompilerRejectsViewportBeforeRendererWithPartDiagnostic`, `TestMotionValidatorMapsTrustedRendererFailure`, `TestMotionDerivativeFailurePreservesValidatedHead`, `TestMotionCompilerTrustedChromePreflight`, `TestArtifactV2PreviewRejectsInvalidWorkingStateWithoutExposingBytes`, focused Artifact V2 persistence/API tests |
| Video proposals/render | AI proposals cannot silently become accepted revisions/final renders; source refs/timing remain exact. Native V3 conversion accepts only an exact selected Git head and current project base; source, fallback PNG, and MP4 retain one complete V3 identity, derivative bytes publish cancellation-safely and atomically, renderer duration/FPS and container types are verified, and source Git/replay remain unchanged. Artifact V2 conversion stays a separate restricted sink. Likely attacks are stale/foreign head or base with no proposal/derivative/source mutation, mixed V1/V2/V3 identity, arbitrary non-media bytes, timing substitution, partial derivative publication, or derivative loss on restart | `artifactv3video.Service`, `runtime.artifactV3VideoBridge`, `runtime.artifactV3DerivativeStore`, `videoproject.Service.CreateEditProposal`, `videorender.Service`, and `tool/runtime_manage_video.go` actions `convert_artifact_v3`/`convert_artifact_v2` | `artifactv3video/service_test.go`; `runtime/artifact_v3_runtime_test.go`; `videoproject/service_test.go`; `store/pebble/session_video_edit_proposal_test.go`; `videorender/service_test.go`; `tool/runtime_manage_video_test.go`; isolated `artifact-v3-multipart-e2e --stage video-conversion` remains the final live gate |
| Updates/releases | update cannot activate unverified/unsafe artifacts or hide rollback failure; apply/run remain restricted to owner IPC or an admitted same-origin local Desktop administrative request | launcher/update API/scripts; `isLocalAdministrativeRequest` | update helper/apply tests, local-auth tests, precommit/readiness scripts |

## 11. Test topology and mapping guidance

Tests exist across multiple ecosystems: Go `_test.go` files in the root and `swarmd` modules, TypeScript/React `*.spec.*` files under `web`, shell/static harnesses under `tests` and `scripts`, and live E2E/testbench runners. Benchmarks and contract/static-source tests answer different questions from behavior tests. `docs/testing/test-audit-ledger.tsv` is the two-pass classification authority for the wider inherited corpus; `scripts/run-critical-tests.sh` is the smaller executable build-blocking manifest selected from inspected assertions.

For a critical behavior, build a **requirement-first map**:

1. state the invariant and threat/failure mode;
2. cite the production registration, boundary, service, and store symbols;
3. list direct tests, contract/static guards, integration tests, and live gates separately;
4. inspect assertions and fixtures—not names—to record what each test actually proves;
5. record missing negative, concurrency, restart, cross-account, traversal, and failure-injection cases;
6. run the narrow package/function or the appropriate checked-in `fast`/`deep`/`agents` manifest; never substitute an unbounded module-wide run.

Never use test count as coverage. Never infer “covered,” “secure,” or “passing” from file presence. A route-to-test entry in §8 means “start inspection here.”

### Critical-suite growth rule

Promote tests into `scripts/run-critical-tests.sh` one invariant at a time. Revision 6 promoted existing independently reviewed listener, media cleanup/isolation, image validation/path, source-media traversal/staleness, render ownership, and FFmpeg drawtext tests, and added direct locked-vault middleware rejection. The next high-value gaps are Workspace Action execution lifecycle/TOCTOU, vault interruption and runtime panic redaction, Pebble commit and outbox-crash failure injection, account-isolated developer diagnostics, hermetic HTML renderer network/file escape, media multipart concurrency, deeper FFmpeg filter/control-character confinement, and a hermetic full Task Program executor simulation spanning child launch → committed handoff → parent integration → dependent-stage unlock. Each addition requires inspected assertions, a negative or injected-failure case, a bounded timeout, successful repeated focused execution, an atlas §10 update, and first-/second-pass evidence in `docs/testing/test-audit-ledger.tsv`. Tests that encode retired contracts stay outside the build gate until corrected; do not weaken current production contracts to make inherited tests pass.

## 12. Known uncertainties and fact-check notes

1. `sessions_v3_sync_contract.go` still calls bootstrap/hydrate/stream “future canonical,” while routes and clients are implemented and the repository contract calls them canonical. Treat the routes as current implementation and the wording as stale until intentionally reconciled.
2. `/ws`, `/v1/sessions*`, v2 agent/custom-tool routes, and V3 workset/TUI routes are registered, but registration alone does not identify all active consumers. Follow the consumer verification rule before removal.
3. The provider registry includes OpenAI in addition to the README’s named support list; Copilot is deliberately dormant. Use runtime registration, not README or directory names, for availability.
4. This atlas statically inspected representative handlers, registrations, contracts, authorities, and tests. It did not exhaustively prove each handler’s accepted HTTP methods or every client caller. Handler-level method tables should be generated only from inspected switch logic, not guessed from REST naming.
5. Atlas revisions 1–3 ran no tests and asserted no runtime health. Revision 4 introduced and executed bounded critical manifests; that evidence applies only to the named manifest at that working revision, not to the wider repository test corpus.
6. `dev_mode` session dump is an operator/debug surface, not a tenant-isolated production read: at this revision it accepts an authenticated principal but looks up the requested session by ID without requiring matching `AccountScopeID`. Its authenticated download subpath serves only a private regular file produced in the dump directory by that flow. Keep development mode disabled outside disposable development/test environments and treat this as a security-review item.
7. Admitted Desktop/Tailscale origin persistence across daemon restart was not established strongly enough in this pass; inspect the origin store and restart path before documenting persistence semantics.
8. Video rendering executes local host FFmpeg rather than a retired container sandbox. Its security therefore depends on opaque registered source references, path validation, bounded invocation, and process controls; no stronger OS-isolation claim is made.
9. Mint/activation reporting and authenticated feedback are current implementation and have focused transport/state/startup tests, but the inherited two-pass ledger inventory predates the feedback test file. The workspace-aware `/task` change also materially edited and added backend, TUI, Desktop, and live-runner test symbols after that inventory; affected inherited rows remain `stale-inventory`. Revision 15 source-bound the corrected live runner and the new focused parser rows with independent first/second-pass `keep` verdicts, while the other materially edited inherited task rows still require the maintained full-corpus reinventory workflow. Focused passing evidence does not make the wider audit complete.
10. Initial critical-suite execution exposed inherited tests that encode stale contracts (including mutable system-agent profiles, non-loopback startup, agent creation without a tool contract, legacy empty-user sessions, and routed starts without `agent_name`). The agent-suite audit additionally found stale prose-literal assertions in `runtime_task_contract_test.go` and `service_task_swarm_test.go`, repaired a positive-auth fixture that omitted the now-required tool contract, and exposed obsolete Desktop startup-profile expectations, an extensionless ESM import failure in `desktop-chat-panel.badge.spec.ts`, and independently unstable/failing full `v3-realtime-controller.spec.ts` cases. These remain outside the build-blocking manifests pending two-pass correction; production contracts were not weakened to admit them.
11. The deterministic `fast` tier now gates workspace-aware `/task` admission and client routing while `agents` proves the wider delegation contracts without external providers. The checked-in testbench has provider-backed `task-routing` and `task-program` suites. Those live suites require the configured loopback SSH testbench and are not CI/build gates; passing unit/integration tiers must not be represented as proof of a real provider successfully launching agents.
12. `panic_privacy_test.go` is intentionally a static forbidden-sink guard. It prevents reintroducing obvious stack/panic formatting in two execution paths but does not prove runtime response/log redaction; a dynamic panic-injection test remains required.
13. The pre-merge audit promoted existing deterministic protection for registered media roots and FFmpeg drawtext escaping, but host FFmpeg still has no stronger process sandbox guarantee and HTML capture still lacks hermetic request-interception tests.

### Independent fact-check performed for this revision

The first fact-check used four independent adversarial scopes (API, security, runtime/orchestration, and product boundaries). This subsequent audit used five fresh scopes: API/middleware, V3 durability/runtime, workspace/execution security, artifacts/media, and product/client/revision integrity. Parent reconciliation confirmed the core architecture and exposed additional concrete errors: missing `/v1` and `/v1/workspace` route prefixes, an over-broad vault-gate summary, a nonexistent Action test path, imprecise V3 collection/nested-dispatch attribution, and system-agent/tool-contract citation gaps. Conflicting or weak claims were resolved in favor of registration/implementation evidence or retained as explicit unknowns. Unsupported specifics (notably exhaustive HTTP methods, universal consumers, passing coverage, admitted-origin persistence, and container-like video isolation) remain omitted or unresolved.

## 13. Glossary

- **Attach token:** daemon access token accepted by authenticated local API middleware.
- **Artifact (current V1):** immutable, lineage-bearing managed output stored under session authority; not synonymous with a workspace file.
- **Working Artifact (Artifact V2):** implemented durable authoring aggregate whose real Parts, immutable Part Revisions, Composition Head, Build/Validation Results, Iteration Rounds, and Published Head remain separate first-class objects. Managed Designer authoring uses a context-bound V2 capability, and Desktop now reads the dedicated `/v3/sessions/{session_id}/artifact-v2` catalog/Studio projection for immediate working visibility, real part history, diagnostics, candidates, exact selection, and locks.
- **Checkpoint / attempt:** durable unit and execution attempt inside a session plan.
- **Endpoint cursor:** opaque signed position within one exact sync scope; not a client-comparable integer.
- **Execution epoch:** bounded provider-context interval within a durable session.
- **Live patch:** ephemeral streaming text/reasoning acceleration; durable messages/events remain authority.
- **Outbox:** Pebble-backed ordered records used for durable replay and realtime fanout.
- **Principal:** authenticated user/account authority injected into request/runtime context.
- **Projection:** current durable session view derived and committed with events.
- **Run intent:** durable record of requested/running/terminal execution used for recovery.
- **System agent:** immutable code-owned identity and tool/security contract.
- **Target:** generic Swarm routing/topology destination; not a retired container profile.
- **Workspace Action:** account-owned, workspace-scoped structured executable definition; definition management is not execution.
- **Worktree:** isolated Git checkout/branch used for a session or delegated Coder scope.

## 14. Revision ledger and update template

| Atlas revision | Code revision | Date (UTC) | Scope | Evidence / decision |
|---|---|---|---|---|
| 1 | `5bc65fe066abff78785fd573328c654d94b615fe` | 2026-08-27 | Initial top-down atlas, API families, security/durability map, test guidance | Initial static synthesis; no tests run |
| 2 | implementation `5bc65fe066abff78785fd573328c654d94b615fe`; atlas input `55e63033b0bed7d94bda06694abaf3817615b0ca` | 2026-08-27 | Independent API, security, runtime, and product fact-check; corrected routes, trust exceptions, orchestration and lineage | Four independent review scopes reconciled to implementation evidence; bounded static checks only; no tests run |
| 3 | implementation `5bc65fe066abff78785fd573328c654d94b615fe`; atlas input `2f827cc9c6f64d605dd2f221a12cad1d100e812f` | 2026-08-27 | Fresh five-scope audit; corrected route prefixes, vault exemptions, Action test evidence, nested session dispatch, agent contracts, and update boundary | Five independent Finder reviews plus parent source reconciliation; bounded static checks only; no tests run |
| 4 | working tree based on `2f827cc9c6f64d605dd2f221a12cad1d100e812f` | 2026-08-27 | Introduced atlas governance, sync enforcement, bounded critical fast/deep suites, CI/build wiring, Desktop critical manifest, and direct Action runner security tests | Four independent test/build/governance investigations plus parent assertion inspection; exact validation results recorded in the checkpoint handoff |
| 5 | working tree based on `2f827cc9c6f64d605dd2f221a12cad1d100e812f` | 2026-08-27 | Added an independently audited deterministic agent/delegation tier; tightened agent/auth/outbox/worktree manifest selection and documented live-provider limits | Five independent assertion/authority/gate reviews plus parent source reconciliation; exact fast/deep/agents execution results recorded in the checkpoint handoff |
| 6 | working tree based on `d84cf154a700ece68229eb6d4f800ea2c1824b9e` | 2026-08-28 | Pre-merge security/governance audit; promoted listener/media/image/source/render security tests; added locked-vault rejection; tightened AGENTS.md test purpose/review/registration rules and atlas-sync coverage | Six independent domain audits, two independent promotion reviews, source fingerprint verification, repeated focused tests, full deterministic gate, precommit, and atlas-sync evidence recorded in the checkpoint handoff; live provider proof remains environment-gated |
| 7 | working tree based on `c13216100d1fd2f54d8c916c2f4c41cbc60a1dac` | 2026-08-28 | Reconciled current `dev` mint/activation/feedback, artifact/video/composition/animation and workspace-scope evolution; added no-empty-match enforcement to the critical Go manifest; wired the complete deterministic gate into the single canonical testbench pre-run | Re-read current route/handler/client and critical-runner/pre-run contracts; preserved unfinished ledger status explicitly; exact critical, scheduler, precommit, candidate/testbench, and live bounded validation is recorded in the checkpoint handoff |
| 8 | working tree based on `844056add32863b377e920d6c6fc0036ab86767f` | 2026-08-28 | Reconciled the live pre-run with per-role credit-saving model overrides and current idempotent metadata/run-intent contracts | Inspected runner payload/assertion failures from exact-head testbench execution; updated the canonical runner path and scheduler contract tests; focused and live validation recorded in the checkpoint handoff |
| 9 | working tree based on `737c4a75a259ea46ddbaea7faf8465a4c483bf23` | 2026-08-28 | Activated PR #46 and hardened automatic critical, dependency, and changelog workflows for exact SHA-scoped concurrency and runner-owned temporary storage | Reproduced policy, changelog, and dependency gates from fresh public base/head checkouts; added the required Unreleased changelog entry; exact validation and GitHub check evidence recorded in the checkpoint handoff |
| 10 | working tree based on `6ac90356a297f905d7d16f8d9fe2ee782672d53c` | 2026-08-29 | Corrected PR critical-check checkout/range availability and remediated the newly published CVE-2026-56854 dependency finding | Reproduced the exact failing GitHub steps, verified the unavailable-base failure and live Trivy finding, then updated exact-head checkout/fetch behavior and the affected daemon module; focused validation recorded in the checkpoint handoff |
| 11 | working tree based on `00c33c48bb21c144de56901692d50b84516bbd6e` | 2026-08-29 | Reconciled the clean-runner prerequisite exposed by exact-head critical checks | Both exact comparison SHAs fetched successfully in GitHub while the first repository-policy command failed before emitting its banner; verified the current Ubuntu runner image omits `rg`, then added explicit ripgrep installation before policy execution |
| 12 | working tree based on `48d4773f` | 2026-08-29 | Grouped Desktop Iteration Swarm results by durable initial-wave or chain-turn identity with part-aware drill-in | Inspected task artifact allocation, V3 catalog projection, Artifact Studio grouping, sidebar/viewer routing, and focused test assertions; no tests run per request policy |
| 13 | working tree based on `48d4773f` | 2026-08-29 | Separated named iteration groups, one first-focused In Progress chain, and supporting assets; added exact group-key viewer routing | Inspected the pure catalog projection, sidebar/gallery/pane navigation state, URL parsing, and model/UI tests; focused TypeScript check and 32 route/model/sidebar tests passed |
| 13 | working tree based on `8978786b` | 2026-08-29 | Required reviewed preflight for every declared HTML animation, added `motion_ui` artifact-owned live-playback proof, and tightened managed Designer group/candidate/target lineage acceptance | Focused htmlcapture and manage_artifact tests passed; run-package guidance test was blocked by an unrelated pre-existing worktree-service test-interface compile mismatch; atlas-sync evidence recorded in the checkpoint handoff |
| 14 | working tree based on `dd422a3389b3` | 2026-08-29 | Made delegated Coder workspace scope immutable so out-of-worktree access fails before user permission creation | Inspected Coder tool contract, runtime worktree resolution, scope-expansion gate, permission creation, and referenced session evidence; focused tool/run tests and atlas sync recorded in the checkpoint handoff |
| 14 | working tree based on `dd422a3389b3b2ab985cf2b561141f93dbbceb6a` | 2026-08-29 | Tightened `motion_ui` authoring guidance after a live focused candidate failed trusted viewport containment; required fully in-frame elements and pseudo-elements at representative timestamps | Exact live failure `animation_viewport_overflow` retained; focused guidance assertions added, but the run-package test remains compile-blocked by the unrelated stale worktree-service test stub; atlas-sync and final bounded evidence recorded in checkpoint cp-4 |
| 13 | working tree based on `8978786b` | 2026-08-29 | Made routed and deployed Git-backed sessions use mandatory session-owned worktrees and added provider-start owner/path/branch revalidation | Inspected routed/deploy admission, V3 atomic publication, workspace grants, provider execution context, worktree inspection, and focused negative tests; exact focused Go and atlas-sync evidence recorded in the checkpoint handoff |
| 14 | working tree based on `837f904b` | 2026-08-29 | Restored workspace-aware `/task` routing across backend, TUI, and Desktop with Router-owned title/worktree seed and mandatory selected-repository isolation | Inspected route registration, replay-before-Router ordering, Router prompt/decoder, allocation rollback, TUI/desktop workspace resolution and authority payloads, provider-start revalidation, and focused backend/TUI/Desktop assertions; focused validation plus atlas-sync recorded in the child handoff |
| 15 | working tree based on `1a0b5c35` | 2026-08-29 | Extended the live `task-routing` proof to current and explicit saved workspaces for Auto/Plan, repaired its retired metadata assertion, and promoted the independently reviewed deterministic backend/TUI/client/Desktop `/task` coverage into the PR `fast` gate | Live exact-head testbench first exposed stale fixture binding selection, then the retired `managed_worktree_requested` assertion; the canonical workspace override reached the product and isolated the harness defect. Independent assertion-level review confirmed replay, rollback/no-authority, workspace-selection, mode, and owned-worktree coverage before manifest promotion; focused/live validation is recorded in the checkpoint handoff |
| 16 | working tree based on `7f62d80f` | 2026-08-30 | Corrected guarded session-lane promotion so an isolated run can reach its exact captured account-owned checkout without broadening ordinary mutation scope, and made integration cherry-pick the source session's captured-base range onto the exact current target HEAD | Inspected tool scope resolution, principal-backed workspace containment, captured session lineage, and worktree preflight/apply rollback. Focused exact-target and foreign-target promotion tests plus atlas-sync evidence are recorded in the checkpoint handoff |
| 17 | working tree based on `0c1349f9` | 2026-08-30 | Restored ordinary Desktop new-session creation to the existing Router-owned routed transaction and removed client worktree-name authority | Traced the regression in `9f1dfd02`, reused strict Router title/worktree naming and one collision retry, preserved atomic worktree-before-session publication and provider-start validation, and updated focused routed API/Desktop tests; exact validation is recorded in the checkpoint handoff |
| 18 | working tree based on `189f3e85` | 2026-08-30 | Made guarded promotion failures actionable for AI callers by requiring prompt-visible full OIDs and distinguishing inspection, dirty source/target, and stale source/target lineage while preserving every no-apply preflight | Canonical API evidence showed an abbreviated source OID, a dirty target misreported as stale lineage, a failed out-of-scope inspect, repeated refreshes, and eventual success only after the target became clean. Focused stale-source, stale-target, dirty-target, exact-target, and foreign-target tests plus atlas-sync evidence are recorded in the checkpoint handoff |
| 20 | working tree based on `67d63734` | 2026-08-30 | Corrected Desktop review-worktree promotion so an exact captured path/base branch remains eligible when the captured checkout advances beyond the session's historical base commit; current target branch/full HEAD remains separately authenticated immediately before apply | Canonical API evidence recorded a captured source workspace on base branch `dev` with historical base `8f1cfeb3…`, while the exact clean captured checkout had advanced to `67d63734…`; focused advanced-target plus foreign-path/branch regression and atlas-sync evidence are recorded in the checkpoint handoff |
| 19 | working tree based on `8f1cfeb3` | 2026-08-30 | Restored prompt-free path-tool access across the user's complete active account-saved workspace pool while keeping unsaved paths permissioned and Coder mutation scope immutable | Durable session evidence proved an erroneous `read` permission for an account-saved workspace; inspected catalog resolution, runtime scope, tool path classification, and permission creation. Focused pooled read/list/search/find/write/edit/webdownload/task-selector and negative external-path tests plus atlas sync are recorded in the checkpoint handoff |
| 21 | source audit based on `b1d22ef8a`, integrated as `8c5ea423` | 2026-08-30 | Audited and reconciled concurrent streamed HTML capture, allowlisted quality/FPS, durable background render jobs, list/status/ETA/cancel/recovery/reuse APIs, and the Video Studio quality dialog/render center | Inspected `htmlcapture`, `videorender`, `videoproject`, V3 store/API/runtime wiring and startup/shutdown recovery, Desktop render consumers, and focused assertions. Corrected Desktop request/response fields and presets to the canonical backend wire contract; no tests or validation commands were run in the delegated source audit. |
| 22 | working tree based on `48e13088` | 2026-08-30 | Removed the duplicate daemon-local `startVideoRenderRecovery` declaration introduced during integration and retained the existing dedicated, tested startup-recovery helper as the single runtime owner | Re-inspected the daemon call site, both declarations, the dedicated helper history, and its nil-service test; `cd swarmd && go build -o "$TMPDIR/swarmd-build-check" ./cmd/swarmd` passed. |
| 23 | working tree based on `d7f19124` | 2026-08-30 | Made curated discovery containment tests hermetic across protected production defaults and CI runner-home placement without weakening storage-root rejection | Authenticated PR job evidence showed six ScanScope tests failed before their assertions on `/var/lib/swarmd` permissions or forbidden runner-home data roots; re-read `discovery.Service.ScanScope`, rooted rule/skill reads, `appstorage.DataDir`, and `storagecontract.ValidateRoot`, then used explicit test HOME/data roots and passed focused normal plus GitHub-like forbidden-home execution. |
| 24 | working tree based on `2c418589` | 2026-08-30 | Extended the same hermetic storage fixture boundary to curated image-generation no-write and managed-path tests without weakening production storage containment | Exact-head GitHub evidence showed discovery passed and exposed two imagegen tests failing before assertions because STATE_DIRECTORY was beneath runner HOME; re-read the shared imagegen harness, `appstorage.WorkspaceDataDir`, non-PNG validation, asset resolution containment, and `storagecontract.ValidateRoot`, then separated synthetic HOME from the explicit data root. |
| 25 | working tree based on `d3e4fb33` | 2026-08-30 | Normalized every canonical testbench live runner and isolated release-gate Plan/Auto cycle onto one explicit Fireworks-only model posture sourced from the ignored `.env` | Inspected `.env` parsing, launch pre-run wrappers, runner recommendation fallbacks, remote gate source installation, isolator arguments, onboarding harness, and account model snapshot assertions. Added fail-closed role validation and a rendered root-owned testbench config; focused local scheduler/runner and private gate contracts passed. Live installation and execution remain separately approval-gated. |
| 26 | working tree based on `83586426` | 2026-08-30 | Removed the Codex-era `service_tier=fast` override from Fireworks testbench assignments and reconciled automatic gate retry/diagnostic behavior | Inspected exact Fireworks catalog IDs, request paths, supported standard/priority tiers, runner assignment builders, the five-minute watcher lifecycle, sanitized diagnostic reduction, and same-SHA discovery suppression. Focused launch, gate, retry-suppression, and diagnostic contracts passed; the automatic timer remains stopped pending reviewed installation. |
| 27 | working tree based on `1b46e466` | 2026-08-30 | Prevented same-candidate pull-request dependency scans from cancelling the separately required push scan | GitHub evidence showed the push job was cancelled during setup while the pull-request job for the same SHA succeeded; inspected workflow trigger and concurrency authority, then scoped the concurrency group by event type plus exact SHA. Focused workflow/atlas validation is recorded in the checkpoint handoff. |
| 28 | working tree based on `2cf2b320` | 2026-08-31 | Preserved delegated children’s durable parent task-call identity through V3 sync shell projection and grouped same-call siblings together in the Desktop sidebar | Canonical API session dumps showed task-program siblings shared `parent_task_call_id` in child metadata while the sync shell allowlist omitted it; inspected task child creation, sync shell projection, Desktop lineage/tree/rendering, and focused backend/Desktop assertions. |
| 29 | working tree based on `3819e2e5` | 2026-08-31 | Distinguished managed Designer render failures from trust rejection and added one bounded author-correction round for eligible animation defects | A canonical development API session dump showed valid parent/child/task/iteration lineage but terminal `failure_code=animation_viewport_overflow`; inspected placeholder allocation, tool failure control, artifact authority lookup, strict handoff checks, and viewport audit. Eligible authored animation failures now receive one fresh immutable destination in the same collection and exact failure guidance; failed candidates remain terminal, a second failure stops, infrastructure/authority failures do not retry, non-Designer agents are unchanged, and ready/lineage/composition/output/profile enforcement remains fail-closed. |
| 30 | working tree based on `9fd8c67a` | 2026-08-31 | Tightened bounded managed Designer correction guidance to preserve already-validated animation contracts instead of inviting broad source reconstruction | Canonical parent and child API dumps showed a viewport-only first failure followed by a replacement that rewrote a valid parser-time runtime into malformed JavaScript and failed `animation_runtime_missing_before_dom_content_loaded`. The refinement feedback now states that reaching viewport audit proves earlier runtime binding succeeded and requires the smallest containment-only edit while preserving the classic head bootstrap/runtime, manifests, timeline, bridge, and design. Focused assertions and atlas-sync validation are recorded in the checkpoint handoff. |
| 31 | working tree based on `ffb6c1ed` | 2026-08-31 | Made post-publication managed Designer visual rejection recoverable without rerunning successful slots and clarified animation export profile inheritance | Canonical parent and all three child API dumps showed two valid animations, one honest exit-frame clipping rejection, no replacement attempt despite a resolvable acceptance gap, and two avoidable fallback calls that supplied a source-owned animation profile. Regular partial waves now advertise one bounded replacement action only for fully evidenced `animation_inspection_failed` slots; mixed/authority/publication/program failures remain fail-closed, and manage_artifact schema/errors state that exports inherit the exact source profile. |
| 32 | working tree based on `ffb6c1ed` | 2026-08-31 | Added a canonical focused live proof for one regular wave of three distinct three-part Designer animations | `scripts/runners/designer-artifact-flow.mjs --stage regular3` now configures and restores the exact testbench Swarm/Designer models, creates a fresh session, requires one regular task call with exactly three differentiated assignments, and fails unless exactly three child sessions each publish one distinct ready text/html animation preserving the exact three-section timeline with zero replacement or failed candidate. Node syntax and the bounded offline runner contract self-test passed; the live provider-backed stage remains the canonical final proof. |
| 33 | working tree based on `ffb6c1ed` | 2026-08-31 | Recovered the live three-Designer proof far enough to expose and correct stale binding selection, empty managed scope drift, same-step assistant idempotency, and Fireworks/Kimi tool-result naming | The resumed canonical run rejected a stale topology binding before session creation; generation-aware runner selection then launched one regular three-child wave. Private canonical parent/child dumps showed an initial `owned_scope: []` approval-manifest mismatch, one valid animation, one viewport failure, one parser-time runtime failure, two child runs colliding when pre-tool and terminal assistant fragments reused `assistant:1`, and a parent continuation rejected because Kimi could not resolve an unnamed tool result. The runner now selects only active generation-matched workspace bindings; empty owned scopes normalize to omitted authority; each same-step assistant fragment has a stable distinct logical key; and the canonical tool name survives run continuation plus V3 normalization into the Fireworks tool message. Focused run, API, provider, runner self-test, and atlas-sync validation are recorded in the checkpoint handoff. |
| 34 | working tree based on `f4fa028f` | 2026-08-31 | Tightened motion_ui authoring guidance after the deployed regular3 proof exposed repeated source defects rather than another orchestration failure | The committed candidate deployed cleanly to the loopback-only testbench and one regular task call allocated exactly three managed Designer children once. Canonical private parent/child dumps showed one ready three-part animation, one responsive scale wrapper rejected by the exact 1920x1080 viewport audit, and one large parser-time scene script with an actual `Unexpected token ')'` syntax error before its textual binder call; the bounded correction repeated that malformed source. Guidance and the focused runner now require a direct fixed stage, a minimal early binder before DOM/Canvas/Path2D construction, syntax checking of the complete classic script, and substantive visible content at the exact t=0 inspection frame; managed preflight capacity now follows the same bounded host-size concurrency as capture instead of serializing all three children behind one slot. Focused validation and the final live rerun are recorded in the checkpoint handoff. |
| 35 | working tree based on `081404e2` | 2026-08-31 | Reconciled the regular3 timeout with Fireworks DeepSeek reasoning behavior and restored the canonical `off` testbench thinking choice | Canonical private parent/child dumps showed all three Designer provider streams remained in first-turn high reasoning for roughly eight to nine minutes, emitting about 53k–60k chunks each before two children were still running; the one completed child published one ready artifact before making three redundant reads rejected by the read-byte bound. This was provider generation latency rather than animation preflight or parent aggregation. Checked-in runner and environment validators now accept `off`, already supported by the model catalog and mapped to Fireworks `reasoning_effort=none`, so bounded role-specific live proofs can disable unbounded reasoning without inventing a provider setting. A final bounded live proof then reached terminal aggregation in about six and a half minutes: all three children ran once with Designer thinking off, two produced ready three-part animations, and the third exhausted its one bounded correction after first omitting the manifest and then binding a runtime whose published ready function was still a placeholder. The canonical guidance now explicitly requires the first bound object to expose its final ready/seek/stop functions because trusted preflight freezes that one-shot runtime. Focused validation and the final live result are recorded in the checkpoint handoff. |
| 36 | working tree based on `4cc8052bb10735d113ec84fe2978c7e8b112ec21` | 2026-08-31 | Froze the Artifact V2 from-first-principles replacement constitution and reconciled artifact, V3 projection, trusted render, storyboard, Video Studio, security, and cutover boundaries before implementation | Inspected V1 `manage_artifact` actions/schema/runtime, artifact authority/Git/parts, Pebble records/mutations, managed placeholder/refinement/partial-wave orchestration, Desktop V1 projection, Chrome renderers, storyboard import, and Video Studio proposal contracts solely for anti-derivation/quarantine and primitive audit. Added a closed V2 object/event/capability contract, exhaustive forbidden-dependency matrix, retained-primitive allowlist, per-boundary positive/negative/stale/cross-account/idempotency/failure-atomicity obligations, single-authority cutover, and structurally read-only legacy adapter. Documentation/static validation only; Artifact V2 remained unimplemented at this revision. |
| 37 | working tree based on `4cc8052bb10735d113ec84fe2978c7e8b112ec21` | 2026-08-31 | Implemented the Artifact V2 durable backend domain and sole mutation authority for Working Artifacts, Parts/Revisions, Composition Heads, Build/Validation Results, Iteration Rounds, Published Heads, V3 progress projection, and exact ready reads | Added `artifactv2.Service`, audited private Git blob interface, new `ArtifactV2*` records/indexes, closed `artifact.v2.*` mutation/event kinds, atomic V3 record/event/projection/idempotency/outbox commit, runtime registration without the V1 authority, exact validated read boundary, structural anti-V1 dependency test, and focused restart/cross-account/idempotency/failure-atomicity/stale-CAS tests. Focused tests passed; test-audit batch P1/P2-092 awaits independent review. |
| 38 | working tree based on `4cc8052bb10735d113ec84fe2978c7e8b112ec21` | 2026-08-31 | Replaced managed Designer authoring and validation with the context-bound Artifact V2 capability | Added `artifact_v2_author`, server-created grants/policy snapshots, durable bytes-before-validation, exact repair lineage, deterministic compiler/validator interfaces, candidate failure projection, regular/swarm Working Artifact allocation, Designer profile/tool/prompt cutover, V1 placeholder bypass for Designers, V2 terminal candidate handoff, and partial-failure diagnostics without V1 replacement waves. Focused author/tool/run/agent/store tests and Atlas sync passed; wider inherited suites contain unrelated existing failures and were not used as checkpoint evidence. |
| 39 | working tree based on `4cc8052bb10735d113ec84fe2978c7e8b112ec21` | 2026-08-31 | Added the dedicated Artifact V2 session catalog, Studio read model, user composition commands, and Desktop sidebar/Studio projection | Added `/v3/sessions/{session_id}/artifact-v2` GET catalog/detail plus exact candidate-selection and complete-composition CAS commands; direct V2 store readers; daemon injection; canonical realtime refresh for `artifact.v2.*`; and Desktop V2 normalization, lifecycle groups, real part/revision navigation, diagnostics, candidate preservation comparison, exact selection, locks, and typed iteration prompts. Focused API/store/runtime and Desktop tests passed; a pre-existing provider-cache assertion in the wider realtime spec remains unrelated and was not used as evidence. |
| 40 | working tree based on `4cc8052bb10735d113ec84fe2978c7e8b112ec21` | 2026-08-31 | Replaced freeform animated artifact authoring with the Artifact V2 server-owned motion compiler, validator, preview, and derivative boundary | Added typed scene/behavior inputs; deterministic template-owned manifests/binder/lifecycle/live scheduler/player bridge/reduced motion/fixed stage; exact compiler/template/policy/source/timestamp evidence; safe structured Chrome diagnostics; authenticated Studio preview; durable preview/fallback/MP4 derivative records; and failure atomicity that preserves source heads. Focused Artifact V2, trusted Chrome rendered preflight, store, API, runtime compile, and Desktop type checks passed. The wider inherited Pebble/API suites reported unrelated pre-existing failures and were not used as evidence. |
| 42 | working tree based on `4cc8052bb10735d113ec84fe2978c7e8b112ec21` | 2026-08-31 | Completed the provider-proof launch wiring for Artifact V2 publication and focused iteration | Made `submit_candidate` create an exact idempotent Published Head; added typed `artifact_v2_source` task schema and orchestration; authenticated one published base before opening an Iteration Round; added atomic multi-revision import that maps candidate-local stable part keys to canonical target parts while preserving non-target/locked digests; added the checked-in `artifact-v2-provider-proof` runner for two single journeys, trusted Chrome evidence, pending conversion, and one three-candidate round. Focused domain/store/run/Desktop tests and runner syntax pass. |
| 43 | working tree based on `41d28f79e3b34f52cb3d193ff73588aae0bc854a` | 2026-09-02 | Repaired concurrent Artifact V2 focused-iteration candidate attachment | `AuthorService.FinalizeIterationCandidate` now retries only exact optimistic working/round revision conflicts, reloading authoritative round state on each bounded attempt while preserving all semantic rejection paths. Added a deterministic barrier-backed concurrency test proving two sibling candidates both attach and advance one round to `awaiting_selection`; the provider runner now preserves the created session ID in partial-stage output for bounded resumption; focused test passes repeatedly. |
| 44 | working tree based on `a9f8e4cfbd88e0d744668a8336de8a31996a5b83` | 2026-09-02 | Made the Artifact V2 model-facing write CAS explicit after fresh provider evidence | A pinned Fireworks/DeepSeek Designer correctly declared and wrote the first motion Part, but `inspect_context` did not expose the Composition Head under the model schema's `composition_head_revision` name; it guessed Working Artifact and foreign Part Revision values and exhausted bounded retries before build. `AuthorContext` now emits explicit `composition_head_revision`, while the compiled and delegated Designer contracts require sequential Part writes using that returned value, omit base revision on a target Part's first write, and retry one stale result only after exact reinspection. The checked-in provider runner also disambiguates regular Task placement so launch-only animation/output fields cannot be duplicated at the task-call top level. Focused author/prompt tests and runner syntax prove the contract; source Atlas sync passes. |
| 45 | working tree based on `8ed0ad72d8dd4db4cbfe162fdfb45f747da0b2ac` | 2026-09-02 | Made server-owned submission close a fully authored candidate without another model bookkeeping turn | Fresh Fireworks/DeepSeek evidence showed a Designer can author complete immutable Parts and then end immediately before `request_build`. `AuthorService.SubmitCandidate` now invokes the existing compiler/validator only when the candidate is still `authoring`, has a complete Composition Head, and has no prior build or validation, then publishes only exact valid evidence; incomplete and invalid candidates remain rejected and repairable. Focused tests prove the fallback creates build/validation/publication records without weakening semantic validation. |
| 46 | working tree based on `0454e3ef0bbd71ae1e9ac1e51c24a9420dfb4af8` | 2026-09-02 | Preserved exact non-target bytes when focused Designers redundantly author them | Provider-backed iteration proved one valid candidate was rejected because it included a byte-identical behavior Part alongside its changed scene Part. `FinalizeIterationCandidate` now removes an identical non-target key from the candidate remainder and reuses the canonical parent Part Revision exactly; any digest or lock change still fails closed. Focused tests prove identical preserved bytes attach without import while changed preserved bytes remain rejected. |
| 47 | working tree based on `ff1336edda98c2d7cf4992f9b6d766d78147cf28` | 2026-09-02 | Enforced target-only Part declaration for focused iteration capabilities | Repeated provider evidence showed Designers sometimes declared and rewrote preserved behavior despite a scene-only iteration brief. `AuthorGrant` now carries trusted declared Part keys resolved from canonical target IDs; `DeclareParts` accepts exactly that key set for focused candidates, and run-context cloning preserves it. Extra preserved/new keys fail before bytes are written, while new-artifact grants remain unrestricted by this field. Focused domain/run tests prove the boundary. |
| 48 | working tree based on `a4a2da72` | 2026-09-02 | Kept exact published Artifact V2 heads iterable after derivative bookkeeping and resumed only missing slots in a partial round | Isolated provider evidence proved `convert_artifact_v2` created a fallback derivative and advanced only the mutable Working revision from 12 to 13, leaving the Published Head, Composition Head, and digests unchanged; the subsequent focused swarm was incorrectly rejected as stale. After that fix, a three-candidate provider wave attached two valid candidates and left one failed slot, but a replacement launch could not re-enter the active round. `PrepareIteration` now authenticates the exact Published Head ID plus unchanged Composition Head revision/digests, treats the supplied Working revision as a nonzero lower-bound observation, and reuses a matching open/generating round only when replacement count equals its exact missing canonical slots. Wrong/replaced heads, mismatched rounds, completed rounds, and count mismatches still fail closed. The provider-proof resume stage now discovers that exact published identity while the mutable Working state is `iterating`, matching the production contract instead of requiring `published_view`. Focused domain and run tests pass. |
| 41 | working tree based on `4cc8052bb10735d113ec84fe2978c7e8b112ec21` | 2026-08-31 | Cut managed creative writes over to Artifact V2 and retired V1 create/create_package registration, model-authored runtime/storyboard prompts, placeholders, correction loops, and manual V2-to-video assembly | Added `artifactlegacy.Reader` with a structurally read-only method set; expanded cross-package anti-derivation/registration tests; added one deterministic durable golden journey through invalid repair, targeted iteration, selection, derivatives, publication, and reconnect; and passed trusted Chrome preflight. A live provider-backed regular three-Designer attempt against the installed testbench proved the old V1 runner/deployment still generated V1 staging/ready artifacts and failed its stale lineage assertion, so it is not valid V2 evidence; the testbench must deploy this candidate and replace the runner with V2 route/reference/pixel/conversion assertions before final launch proof. |
| 49 | working tree based on `ffb6c1ed` | 2026-09-01 | Fixed fresh-host installer ownership ordering and optional `TMPDIR`; added exact-candidate Ubuntu/Arch PR/build systemd installation and official-ISO Omarchy testbench coverage | Re-read both installer authorities, release archive assembly, public build workflows, deploy checklist, testbench manifest, and Swarm public-source procedure. Added a narrow ordering regression and bounded distro/VM runners; exact validation and unavailable environment-dependent runs are recorded in the checkpoint handoff. |
| 50 | working tree based on `c6ca6b83` | 2026-09-01 | Corrected the install-distro PR gate to use the repository-verified resolvable `actions/download-artifact` v8 pin | Exact push-run evidence showed both distro jobs failed before candidate execution because the new workflow used an unresolved action commit. Reused the independently established pin from `build-main.yml`; YAML, policy, atlas-sync, and renewed PR execution evidence are recorded in the checkpoint handoff. |
| 51 | working tree based on `b6c38da9` | 2026-09-03 | Made the isolated container the sole automatic live candidate-test target, removed host-service fallback, and completed the managed Designer V3 write cutover | Re-read the canonical `dev` container deploy script, fixed broker/helper, `.env` authority, Artifact V3 live runner, compiled Designer registry, launch manifest, daemon construction, V2 session API, and Desktop V2 surface. The canonical `dev` script now accepts a same-repository clean managed worktree and bundles that worktree's exact HEAD. Managed Designers receive only `artifact_v3_author`; V2 author compiler/validator construction and API/UI writes are gone while authenticated historical reads remain explicit. Exact container builds caught and removed orphaned V2 daemon locals, then exposed two ES-target-incompatible `replaceAll` calls in Artifact V3 Studio; those calls now use the project-compatible global regex form. `build-main-dist.sh` emits a bounded Desktop compiler tail on failure even when the outer broker suppresses ordinary build output. Focused routing, backend agent/tool/API/run/runtime, Desktop, and Atlas checks are recorded in the checkpoint handoff. |
| 52 | working tree based on `ced598ac` | 2026-09-03 | Made dev-only session dumps retrievable through loopback testbench tunnels and automatic on Artifact V3 journey failure; prevented fallible daemon construction from racing store-backed background jobs | Added an authenticated `dev_mode` exact dump-file path constrained to generated private regular-file basenames, extended the canonical helper with local mode-0600 materialization, and invoked it from the Artifact V3 runner before tunnel teardown. The exact redeploy then exposed an existing startup error racing eager artifact maintenance against a closed Pebble store and a failed initial authoring turn's valid owner-bound empty repository being treated as fatal recovery state; background jobs now start only after fallible daemon construction succeeds, while empty repositories remain retryable without projecting success. The recovered dumps then proved preview/finish schema drift: Designers emitted unknown presentation fields, omitted Part labels, and used shorthand selectors. Build/preview now invoke the exact Git project validator and return actionable manifest diagnostics, while Designer guidance shows the canonical locator shape. A subsequent live root commit exposed the deeper opaque-sandbox defect: CSS/JS subresources carry no Desktop cookie/origin and were rejected as JSON, then reported by Chromium as ORB blocks. Artifact V3 now reuses the audited short-lived encrypted preview capability, binds it to session/artifact/exact revision, embeds it in every rewritten resource path, validates it in both Desktop-boundary and auth middleware, and retains CSP, nosniff, CORP, and private no-store controls. The checked-in live runner now requests the same exact preview capability before direct pixel inspection. The next sidebar follow-up exposed a deeper redundant-authority defect: even with those fields visible, the parent repeatedly failed to reproduce the selector already owned by the exact Git manifest. For Artifact V3, artifact_v3_source.target_part_ids is now the authoritative target set; section_target(s) are optional display hints that need only matching IDs, while the coordinator validates every target against the exact head manifest before allocation. Legacy artifact targeting retains strict caller-supplied locator validation. Focused session-dump, Artifact V3 manifest/recovery/preview, runtime construction/restart, agent-guidance, Desktop API, shell/Node syntax, and Atlas checks were run; the disposable broker config separately enables and verifies `dev_mode`. |
| 53 | working tree based on `f8f47ad8` | 2026-09-04 | Reordered Artifact V3 restoration around no-Designer deterministic gates and a minimal primary-Swarm static HTML probe | Re-read the Artifact V3 runtime author/build/preview path, Git/Pebble lifecycle tests, native API/Desktop runner, task target parsing, and V2-only storyboard/video conversion boundary. The restoration audit now requires static HTML and Parts, exact-base continued turns, sibling alternatives, Part animation, and native V3 storyboard/HTML-animation/Video Studio/MP4 lineage before Designer acceptance. The live runner gained a bounded `basic-html` stage that asks primary Swarm for one plain static artifact, asserts no Designer delegation, native replay/realtime/Desktop discovery, exact preview pixels, and zero legacy identity. The target-hint repair keeps exact manifest Part IDs authoritative while legacy locator validation remains strict. |
| 54 | working tree based on `fa017f26` | 2026-09-04 | Restored the two-slot worktree-aware container test client and reconciled all checked-in callers | Compared the stale single-slot client with the reviewed pool implementation and the critical-operations broker contract. The client now derives a stable lane from repository plus worktree, atomically deploys through lane-scoped claims, supports two isolated slots, and keeps heartbeat/reaping semantics. The tunnel and runner wrappers now verify the assigned slot's exact HEAD, deploy only when stale, derive slot-specific ports, export the authenticated Desktop/API URLs, and resolve pinned Playwright dependencies from the repository's primary checkout without installing into the candidate worktree. Updated the routing contract and container runbook; exact slot deployment succeeded and the first basic-HTML run reached the runner before exposing the Playwright path drift corrected here. |
| 55 | working tree based on `dff35895` | 2026-09-04 | Restored one direct primary-Swarm HTML write into native Artifact V3 | Exact pooled E2E evidence showed the ordinary Swarm path could describe HTML but the `manage_artifact` schema omitted `create`; the provider repeatedly substituted `generate_image`, which cannot create HTML and failed the run. Re-read the managed-artifact schema/executor, authenticated provider run context, V3 author service, stable HTML Part derivation, and runtime Git/build/browser projection bridge. `manage_artifact create` now accepts only one complete text/html document, derives stable selector Parts, allocates a context-bound initial V3 turn, runs the same whole-project build/browser preview and finish path, and returns native artifact/commit identity without V1/V2 collection/variant writes. The first ready publication is idempotent for that exact provider run, preventing model-authored "refinements" from creating a second root Artifact. Non-HTML and missing-Part inputs fail before publication. The checked-in E2E now streams named gate state and immediate blockers for artifact count, exact Parts, revision/build/preview, zero Designer children, pixels, Desktop, replay, realtime, and legacy-write checks; pooled tunnel readiness accounts for fixed-broker status/touch latency and preserves bounded startup diagnostics. That evidence exposed a renderer authority mismatch: the server accepted 1920×1080 while the reviewed 1440×900 surface overflowed. HTML capture now supports bounded caller-selected viewports within the fixed maximum, reports `capture_viewport_overflow` before hiding overflow, and Artifact V3 validates its exact 1440×900 presentation before a candidate can finish. The next live gate then exposed native V3 discovery hidden behind an active Plan tab; the session sidebar now explicitly prioritizes the first native V3 artifact arrival so its card and three-Part navigator are immediately visible without requiring a manual tab switch. Once Desktop and durable genesis replay passed, primary Swarm remained running because its text-only action model had no materialized `media_inspect` and native V3 returned no inspectable reference. Direct create now returns `media_inspect_reference {session_id, artifact_id, revision_ref}`; the media tool accepts that native identity, authenticates owner/session/revision, reads the exact digest-bound server capture, and delivers its PNG only to an image-capable resolved model without V1/V2 translation. The basic-HTML runner selects a vision-capable primary model while continuing to assert zero Designer children. The first vision run then exposed two more contract gaps: explicit `parts` were rejected and safe renderer errors were collapsed, causing the model to publish a minimal diagnostic probe. Direct V3 create now accepts requested Part metadata only when every ID resolves to the authored HTML, preserves that requested order/label, and fails before publication otherwise; trusted capture errors expose only their bounded code/message so the same run can repair the real artifact instead of guessing or replacing it with a probe. After native pixel inspection succeeded, terminal completion exposed one more legacy-only boundary: plan artifacts accepted only collection/variant references. `SessionPlanArtifactReference`, its tool schema, normalization, deduplication, and save authentication now accept exact native `{session_id, artifact_id, revision_ref}` handoffs, reject mixed legacy/workspace identity, authenticate the owning V3 session/repository/revision, and attach the ready HTML without inventing a legacy variant. |
| 56 | working tree based on `1a9ece1f` | 2026-09-04 | Kept Fireworks media continuation stable across equivalent credential records without accepting rotated key material | The first live no-Designer root journey reached ready native V3 pixels and terminal handoff, then Fireworks rejected the continuation because its media contract fingerprint included the mutable active credential-record ID. Re-read declaration, active credential lookup, request authorization, and media-contract validation. Fireworks now fingerprints normalized account/provider/type/API-key material, so duplicate records containing the same actual key remain compatible while a changed key is still rejected before provider media delivery. The exact declaration/continuation regression and focused native V3 media/runtime/create/schema tests pass; known unrelated stale legacy media/plan assertions remain outside this repair. The first exact-commit rerun crossed terminal completion and all product-visible artifact gates, then exposed that the runner's realtime prerequisite waited only for socket `open`, not the exact session's cursor-bearing `replay.complete`; the next exact commit passed that strengthened handshake and produced three valid model-named stable Parts, exposing a harness-only literal-ID requirement absent from the ordinary user brief. The gate now authenticates exact manifest IDs/selectors and content semantics without adding internal names. The subsequent exact commit found a genuine overlap; primary Swarm submitted corrected HTML, but silent per-run replay returned the old commit and drove it into legacy actions until timeout. Direct create now explicitly labels exact-byte replay and turns changed corrected HTML with unchanged stable Part IDs into one exact-base native candidate selected after successful build/preview; focused head-before-selection, ancestry, restart, Part-stability, and replay tests pass. The next exact commit passed creation, pixels, Studio, replay, and zero-Designer checks but the harness lost earlier socket frames when it navigated to Studio; its bounded same-origin sessionStorage diagnostic ring now survives document navigation, and another clean-commit rerun remains pending. |
| 58 | working tree based on `a1119d96` | 2026-09-04 | Bound primary-Swarm alternate cardinality to one native Artifact V3 tool call | Exact resumed E2E evidence isolated provider completion after only one of two separately requested `revise_v3` siblings. `revise_v3.alternatives` now accepts 2–16 complete HTML candidates with distinct contiguous indexes, runs the existing exact-base build/preview/commit contract for every sibling in one shared turn, and reports success only after all requested candidates are ready; malformed indexes fail before authoring and head selection remains explicit. The bounded runner now requests both Footer alternatives in that one provider call. The generic runner wrapper now forwards its checked-in initial-run, Artifact ID, and Desktop-path resume inputs so a harness-only late failure can reuse the exact session instead of replaying provider work. Focused tool, Bash, and Node syntax checks pass. Exact isolated execution then durably created two ready direct siblings in one call; the provider run failed only after that product postcondition while attempting unrelated plan/handoff work. The runner now evaluates canonical two-candidate Artifact V3 state before propagating a later run failure. Exact sibling pixel inspection/choice and downstream animation/video gates remain pending. |
| 59 | working tree based on `c95aed32` | 2026-09-04 | Proved account-backed Codex GPT-5.6 Luna/medium for no-Designer native Artifact V3 static, targeted-turn, and animated-Part journeys | The pooled container's fixed proxy had used the Fireworks-only host profile, so both Codex device authorization and token exchange deterministically returned 403 before credentials existed. The critical-operations testbench profile now retains its fixed runtime hosts and permits only `api.openai.com`, `auth.openai.com`, and `chatgpt.com` for user-driven OAuth and account-backed inference; every other destination remains rejected. Device-code login succeeded in disposable slot state, redacted provider/catalog reads proved Codex runnable and exact `gpt-5.6-luna` availability, and Swarm/Designer assignments persisted at medium reasoning. Exact commit `c95aed32` passed fresh no-Designer basic HTML and targeted Pricing candidate journeys. The bounded `animated-parts` stage then created one three-Part artifact with one running visible animation per Part, advancing timelines and changing computed motion-marker signatures, two nonidentical inspected 1440×900 frames, substantive Studio iframe pixels, durable replay, cursor-bearing realtime, and zero legacy writes. Runner inspection uses rendered `innerText` and waits for substantive iframe content so DOM concatenation or blank timing-race screenshots cannot satisfy visual gates. A clean slot redeploy correctly clears disposable product identity/OAuth, so the repeatable sequence is exact-head deploy, loopback Desktop bootstrap, device-code login, model assignment, then test without redeploy. Native V3 storyboard/Video Studio/MP4 conversion remains the next RED boundary. |
| 60 | working tree based on `f308d96a` | 2026-09-04 | Finished native Artifact V3 animation-to-Video-Studio/MP4 wiring and added the resumable no-Designer conversion gate | Re-read native Git head/evidence authority, Video Studio plan validation/storage, renderer materialization, manage_video schema/runtime, daemon wiring, and the no-Designer runner. The server now binds renderer-observed duration/FPS into conversion validation, rejects invalid PNG/MP4 containers before atomic publication, exposes canonical pending proposal/base/working IDs plus explicitly labeled bounded digest-verified derivative container evidence, and tests exact bridge conversion plus derivative/proposal restart. The runner can create or resume an animated V3 head, request only server-owned V3 conversion, inspect pixels, require one pending native proposal and bounded derivative container/size evidence, and assert unchanged source/replay with zero Designer or legacy identity. Focused package, harness syntax, diff, and Atlas validation are required for this implementation; the isolated live stage remains parent-owned. |
| 57 | working tree based on `46fa0d95` | 2026-09-04 | Passed the first exact no-Designer primary-Swarm HTML gate and opened the native targeted-Part gate | Exact isolated-container evidence passed one Artifact, three stable selector Parts, 1440×900 pixels, Desktop Studio, two durable genesis events, two cursor-bearing realtime events, zero Designer children, and no legacy identity. The next gate exposed no primary-Swarm follow-up authoring surface: Studio generated an exact native reference, but existing follow-up guidance routed only through Designer task orchestration. Added authenticated bounded `read_v3` and exact-base `revise_v3` manage_artifact actions; revise preserves ordered Part IDs, builds/previews the complete HTML, publishes one direct-child candidate, returns its inspectable reference, and leaves head selection explicit. The checked-in runner gained a `targeted-part` no-Designer stage; its first exact run reached valid root pixels and exposed a harness-only literal sibling-ID assertion, now replaced by semantic target resolution from the exact manifest. Focused tool/runtime tests pass. The next exact run durably created and pixel-inspected the candidate, but the harness raced detail projection visibility after terminal run intent; a bounded 30-second canonical-detail poll now waits for the exact-base turn to reach `awaiting_selection`. The next run exposed a valid prior visual-repair turn; the harness now records prior turn count and requires exactly one added targeted turn rather than assuming two total. The next run reached valid root/candidate pixels and Studio visibility but expected a historical warning when returning from an unselected candidate to the still-current root; it now asserts exact root preview/button identity instead. The next exact retry failed earlier when the provider exhausted its bounded root run after two overflow attempts and produced zero Artifact records; one materially identical source retry was permitted and reached every targeted product/Studio/pixel gate. Its final replay assertion reused a bounded hydrate slice; the runner now uses the canonical paginated event endpoint and distinct root/candidate screenshot files. Exact commit `28d0ef97` then passed root/candidate pixels, one added Pricing turn, unchanged head, direct-child ancestry, Studio history/prior preview, five durable events, five cursor-bearing realtime events, zero Designers, and no legacy identity. The next bounded stage now selects that exact candidate through Studio and requires a new primary-Swarm Hero candidate to parent the selected commit while retaining the Pricing change; its first exact run stopped when implicit Part derivation included an extra semantic wrapper. Direct create now rejects more than three implicit Parts and requires the model to name exactly the three requested Parts. The next run exposed a declared footer clipped below an overflow-hidden viewport; trusted capture now receives server-authored selector Parts and rejects any required Part that is invalid, missing, invisible, or outside 1440×900 before readiness. Real-Chrome and focused runtime tests pass. Two subsequent runs reached Studio selection and returned `artifact v3 git: invalid input`: Desktop used a colon in its selection request ID while Git transaction IDs forbid it. Desktop now uses the canonical hyphen-safe form; its API spec passes 4/4. Exact commit `8262fdf1` then passed Studio selection and a primary-Swarm Hero continuation directly parented to the selected Pricing commit, retained both requested labels in inspected 1440×900 pixels, and recorded ten matching durable/realtime native events with zero Designers or legacy identity. Native `revise_v3` now also accepts a stable shared turn key plus distinct bounded candidate indexes for complete sibling alternatives from one exact base; the next runner stage inspects both, keeps head unchanged, and chooses one explicitly. Its first run reached selected Pricing state but the harness failed to recognize exact Part ID `hero` under label `Product introduction`; target resolution now prioritizes manifest ID authority. The next exact run timed out inside a trusted-overflow repair on the already-proven Pricing step without publishing an invalid candidate or advancing head; one exact-source retry then failed before creation when Desktop realtime did not open within 30 seconds. Recovery traced the latter to a full host filesystem, removed only rebuildable Go cache state, restored the exact-session realtime handshake, and added a bounded resume stage rather than replaying green prerequisites. That stage opens collapsed historical turns before selection, rejected and repaired an overflowing continuation candidate before selecting it, and reached real same-base Footer siblings. The provider-backed gate remains RED. Split one-candidate provider turns produced two real siblings on one run, but accumulated same-base history first made the runner select an older turn. A before/after turn-set filter removed that ambiguity; the next exact run proved one provider turn completed without attaching its requested candidate, leaving no new two-candidate turn. Provider execution/cardinality at the isolated sibling gate remains the first open boundary; animation and storyboard/video have not started. |

Copy this row when updating:

```text
| N | `<commit>` | YYYY-MM-DD | `<changed domains/routes/invariants>` | `<paths/symbols inspected; tests or gates actually run; unresolved items>` |
```

Update checklist:

- [ ] Base commit recorded and worktree clean before inspection.
- [ ] Changed routes reconciled to `Server.apiMux` registrations and handler method logic.
- [ ] Changed clients searched in Desktop, TUI, scripts, and tests.
- [ ] Storage/mutation and auth/scope boundaries re-read.
- [ ] Current/compatibility/experimental/retired labels revalidated.
- [ ] Relevant tests inspected by assertions; no coverage claim inferred.
- [ ] Validation actually run is listed; skipped validation is explicit.
- [ ] `bash scripts/check-atlas-sync.sh` passes.
- [ ] If a §10 invariant or its evidence changed, `scripts/run-critical-tests.sh` and the two-pass audit ledger were reconciled.
- [ ] Known uncertainties updated rather than silently removed.
