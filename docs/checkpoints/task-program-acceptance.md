# Task Program acceptance result

Tested implementation: `be7762e503faa6dc46f590620920617282bab693`.

## Observed results

| Requirement | Result |
| --- | --- |
| One approved plan, ten ordered checkpoints | All ten checkpoint runs completed |
| Canonical attached programs | Ten `task action=start` calls without reconstructed program objects completed |
| Interleaved inline programs | Two additional programs completed inside checkpoints 3 and 7 |
| Mixed agents | Finder research, two native Designer alternatives, scoped Coder consumer and Finder audit completed |
| Child outcomes | 25 jobs completed/integrated, each on its first attempt |
| Two repository destinations | Alternating Coder integration destinations verified from retained commits and branch contents |
| Source protection | Both captured repository heads and working trees remained unchanged |
| Native artifacts | Both exact final previews inspected at 960×540; requested text legible, no clipping, overlaps, scrollbars or extra copy |
| Finder-to-Finder regression | A separate two-stage live chain consumed the authenticated preceding report and completed without attempting parent orchestration |
| Observer failure behavior | Seven deterministic tests passed; real blocked and duplicate-program outcomes returned immediately with nonzero status |
| Persistent rebuild | Same-lane setup configuration remained byte-identical; onboarding stayed complete and provider connection remained available |

The final ten-checkpoint observer recorded no failures. Earlier unsuccessful observations and recovery programs remain historical evidence, not rewritten successes. The recovery confirmation reused the completed audit report without replaying the committed write or prior audit. Its report explicitly retained the limitation that the original audit did not provide a commit identity; commit identity was independently available from durable program/Git evidence.

## Validation and boundaries

Focused Go prompt/handoff regressions, JavaScript observer tests, syntax checks, atlas synchronization and the bounded critical fast build gate passed. The control-workspace rebuild helper has three hermetic preservation tests, including restoration of exact configuration bytes after installer defaults.

This establishes the bounded tested workflow, not universal provider reliability, exhaustive abrupt-process-crash coverage, or production release authorization. Earlier pause/block/restart probes are retained separately; an immediate pause/restart once left an active run requiring canonical stop/resolution, so no blanket crash-recovery claim is made. No source was pushed, promoted to the integration branch, or released.

## Relevant paths

- `scripts/runners/task-program-ten-checkpoints.mjs`
- `scripts/runners/task-program-mixed-handoffs.mjs`
- `scripts/runners/task-program-observer-state.mjs`
- `scripts/runners/task-program-observer-state.test.mjs`
- `swarmd/internal/run/service_task_child_assignment.go`
- `swarmd/internal/run/service_task_child_assignment_test.go`
- `swarmd/internal/run/service_tools.go`
